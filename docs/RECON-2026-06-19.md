# MyGridTime — Full Recon Report

**Generated:** 2026-06-19  
**Method:** Read-only reconnaissance. No files edited, no git writes, no deploys, no migrations run.  
**Real repo:** `C:\projects\mygridtime-app`  
**Canonical remote:** `https://github.com/mickbarlow78/mygridtime-app.git`

---

## FOLDER MAP

Four `mygridtime*` folders exist under `C:\projects`:

| Folder | Git? | Remote | Branch | Commits | Role |
|---|---|---|---|---|---|
| `mygridtime` | Yes | `https://github.com/mickbarlow78/mygridtime.git` | `main` | 3 | Early scaffold / throwaway — NOT the real repo |
| **`mygridtime-app`** | **Yes** | **`https://github.com/mickbarlow78/mygridtime-app.git`** | **`mgt-107-rehearsal`** | **113** | **THE REAL REPO — canonical GitHub remote, full history** |
| `mygridtime-deploy` | No | — | — | — | Static marketing site files (HTML/CSS/JS + Node proxy for early-access form); not a git repo |
| `mygridtime-website` | Yes | `https://github.com/mickbarlow78/mygridtime-website.git` | `master` | 22 | Separate marketing website project |

**Verdict:**
- `mygridtime-app` is the unambiguous real working repo — it matches the canonical GitHub remote, has 113 commits of active development history, `node_modules` installed, and a `.next` build output present.
- `mygridtime` has 3 commits and a different remote (`mygridtime.git`, not `mygridtime-app.git`) — this is a stale early test repo with no application value.
- `mygridtime-deploy` is a manual artefact: a static landing/marketing site for `mygridtime.com`, served separately from the Next.js app. It has no git history and no deploy pipeline visible from the folder itself. Its Netlify site (`e974a27c`) is linked to a GitHub repo named `lights-out` — likely a legacy or renamed repo reference.
- `mygridtime-website` is a separate project for the public marketing website (`22 commits, master branch`) and is not the application code.

**All further recon below concerns `C:\projects\mygridtime-app` exclusively.**

---

## SUMMARY

MyGridTime is a multi-tenant motorsport timetable management application built on Next.js 14 (App Router), Supabase, and Netlify. **The owner's belief that a semi-working system exists is a significant understatement.** The system is substantially complete: Phases 0–6 are fully implemented and committed, comprising Supabase auth (magic link), a full admin CRUD suite (championship management, event editing, drag-drop timetable builder, member invites, version history, templates, audit log), multi-organisation architecture, transactional email notifications via Resend, a public timetable view, and early-stage AI timetable extraction via Claude Vision. Phase 7b (AI extraction) is partially shipped. Phases 7a (PWA/Web Push), 7c (Stripe), and 7d (community) are stubs or absent. The critical in-flight work is the `mgt-107-rehearsal` branch completing an `organisation → championship` rename: the code rename is done and the DB migration exists — and the remote Supabase DB shows that migration **already applied** at 2026-04-24, despite the latest commit message labelling it `[NOT YET APPLIED]`. This contradiction is the most important state to verify before proceeding.

---

## WHAT EXISTS AND IS CORRECT

### Git and source control
- All application source is committed and pushed. Zero untracked source files. No stashes. The working tree is clean (4 "modified" files are Windows CRLF line-ending noise only — no real content changes).
- 113 commits of coherent history from Phase 0 scaffold to the current `mgt-107-rehearsal` ticket series.
- Remote branch `origin/mgt-107-rehearsal` exists and matches local HEAD.

### Codebase — implemented and functional

**Phase 0 — Repo / Deploy foundation:** Complete. Next.js 14, Tailwind, TypeScript, `@netlify/plugin-nextjs`, Sentry (client/server/edge configs), PWA manifest and icons, robots.txt, dynamic sitemap.

**Phase 1 — Auth:** Complete. Supabase magic link login, OAuth callback, middleware session refresh, admin route guard, environment validation on startup, dev-only auto-login route (disabled in prod).

**Phase 2 — Database schema:** Complete. 16 applied migrations covering all tables with RLS policies. Hand-written TypeScript types in `src/lib/types/database.ts`.

**Phase 3 — Admin CRUD:** Complete and hardened. Championship create/switch/settings (branding, name, slug, members), event full lifecycle (create/edit/publish/unpublish/archive/duplicate), drag-drop timetable builder (`@dnd-kit`), invite system, member management with role control, version history (timetable snapshots), templates, audit log (event-scoped and championship-scoped). All server actions harden against raw Postgres errors with Sentry capture.

**Phase 4 — Public timetable:** Complete. Public landing at `/`, championship timetable list at `/[slug]`, per-event public view at `/[slug]/[eventSlug]`, print views, privacy/terms pages. Permanent 308 redirects from old `/o/[slug]` to `/[slug]`.

**Phase 5 — Email notifications:** Complete. Resend integration, `sendEventNotification()` with 10-minute debounce, HTML + plain-text email templates, `notification_log` table, opt-in triggers (publish dialog + "Save and notify" modal), token-based unsubscribe at `/notifications/unsubscribe/[token]`, `notification_preferences` table with `List-Unsubscribe` header.

**Phase 6 — Multi-org / Templates:** Complete. Multi-championship architecture (cookie-based active championship), championship selector in admin, template save/load/delete at `/admin/templates`, `createEventFromTemplate()` with cascade-delete rollback on partial failure, first-org onboarding redirect.

**Phase 7b — AI extraction (partial):** Phase A (mock) and Phase B (real Claude Vision tool-use) are shipped. `src/lib/ai/extract.ts` implements `extractWithClaude()` using `@anthropic-ai/sdk ^0.39.0`. Server actions for upload/extract/preview/create-from-extraction exist. `ai_extraction_log` table and storage bucket for PDFs are in place. 20-extraction-per-org-per-24h rate limit implemented. AI gate driven by `NEXT_PUBLIC_AI_EXTRACTION_READY` (derived from `ANTHROPIC_API_KEY` presence at build time). Retention cron at `/api/cron/retention-extractions` is implemented.

### Infrastructure
- `netlify.toml` is minimal but correct: `npm run build`, `.next` publish directory, Node 20, `@netlify/plugin-nextjs` plugin.
- Two Netlify sites exist under the LB42 account: `mygridtime-app` (`app.mygridtime.com`) wired to the `mygridtime-app` GitHub repo, and `mygridtime` (`mygridtime.netlify.app`) for the marketing site.
- Supabase CLI is authenticated. Both Supabase projects (`mygridtime-app` production NANO, `mygridtime-staging` MICRO) are accessible, both in `eu-west-1`, both on PostgreSQL 17.6.x, both `ACTIVE_HEALTHY`.
- All 17 local migration files match what is applied to the production Supabase project. No local-only un-applied migrations visible from production's perspective.
- `ANTHROPIC_API_KEY` is present in `.env.local` — AI extraction works locally.
- `RESEND_API_KEY` is present in `.env.local` — email notifications work locally.

### Test suite
- 79 unit tests covering utility functions, email templates, audit helpers, slug/time/URL utilities, AI extraction validation, Resend client, retention logic, and the CollapsiblePanel component.
- Vitest configured correctly, test files alongside source under `src/**/*.test.{ts,tsx}`.

---

## WHAT IS MISSING OR INCOMPLETE

### Phase stubs and absent phases

| Phase | Status | Detail |
|---|---|---|
| 7a — PWA / Web Push | Scaffolded stub only | `sw.js` is a 4-line comment placeholder. No `web-push` npm package. `VAPID_*` vars declared but unused. `/my/alerts` and `/my/drivers` are "Coming soon" pages. |
| 7c — Stripe / Paid Alerts | Empty stub | `api/webhooks/stripe/route.ts` returns 501. No `stripe` npm package. No subscription gating logic. `users.subscription_status` column exists in DB but unused in code. |
| 7d — Community Updates | Absent entirely | Spec document exists in `project-docs/` but no routes, components, migrations, or hooks exist. |

### Consumer area
- `src/components/consumer/` and `src/components/timetable/` are empty (`.gitkeep` only).
- `/my/upload` is a "Coming soon" stub — consumer-side AI extraction upload not yet implemented.

### No custom React hooks
All data-fetching is server-side (Server Components + Server Actions). No `use*.ts` hooks exist. This is intentional architecture, not a gap, but worth noting for any client-side interactivity work.

### No Supabase-generated TypeScript types
No `database.types.ts` generated via `supabase gen types typescript`. Type safety depends on the hand-maintained `src/lib/types/database.ts`. If schema evolves and types are not manually updated, type errors will be silent.

### No Edge Functions
`supabase/functions/` contains only `.gitkeep`. No Edge Functions defined.

### No E2E tests
Only unit tests exist. No Playwright, Cypress, or similar. No Supabase mock — all tests are pure utility-level.

### Marketing site not version-controlled
`C:\projects\mygridtime-deploy` has no git repository. Changes to the landing page, legal pages, and early-access form are untracked. The `comingSoonGate: true` flag in `site.config.js` is hardcoded — disabling the gate requires a code change and manual redeploy.

---

## DRIFT AND RISKS

### CRITICAL — MGT-107 migration status contradiction

The `mgt-107-rehearsal` branch is 2 commits ahead of `origin/main`. The penultimate commit (`e6e6ebc`) is labelled `[NOT YET APPLIED]` in its message, referring to migration `20260424000000_mgt_107_rename_org_to_championship.sql`. However, the live Supabase production database reports this migration as **applied at 2026-04-24 00:00:00**. 

**Possible explanations:**
1. The migration was applied to production directly from the feature branch (via `supabase db push`) while the code merge to `origin/main` is still pending.
2. The commit message is stale — it was accurate when written but the migration was applied afterwards.

**Risk:** The production DB already has `championships` / `championship_members` / `championship_id`. The code on `origin/main` includes MGT-104/105/106 (championship rename in code) but NOT the `mgt-107-rehearsal` branch commits. If the deployed Netlify build is from `origin/main`, it now has championship code talking to championship DB tables — this is probably fine. But if any path still references `organisations`, it will 500 in production. **This needs immediate verification: confirm which commit is actually deployed to Netlify production.**

### HIGH — `seed.sql` uses old table names

`supabase/seed.sql` still inserts into `organisations` and uses `org_id`. After MGT-107 migration, running `supabase db reset` locally will fail at the seed step. This breaks local development setup for any new contributor.

**Remediation:** Update `seed.sql` to use `championships` and `championship_id`.

### HIGH — `.env.local` points to production Supabase

The active local development environment (`NEXT_PUBLIC_SUPABASE_URL` in `.env.local`) connects to the **production** Supabase project (`hxxderwxxpfzdxlmsqpl`). Local development writes and destructive operations go to the live database. There is no staging-pointing local env file.

**Risk:** A local `db reset`, bad seed, or accidental data mutation will hit the production database.

### MEDIUM — `mgt-107-rehearsal` not merged to `origin/main`

Two commits exist on the feature branch but not on `origin/main`:
1. `e6e6ebc` — MGT-107 DB migration commit
2. `b661be9` — "rebuild preview" (appears to be an empty marker commit to trigger Netlify redeploy)

The branch is already in sync with its remote (`origin/mgt-107-rehearsal`). It needs a PR merge to main.

### MEDIUM — Netlify project not linked locally

The `mygridtime-app` folder is not linked to the Netlify site via `netlify link`. The per-context Supabase env var names and values (Production vs Deploy-preview vs Branch) cannot be audited from the CLI. It is unknown whether deploy previews correctly point to staging Supabase or accidentally use production credentials.

### MEDIUM — No per-context env var overrides in `netlify.toml`

All environment variable configuration for all deploy contexts is managed solely in the Netlify dashboard. There is no code-visible record of which Supabase project (production NANO vs staging MICRO) each deploy context targets. This is a governance and auditability gap.

### MEDIUM — Staging migration history unverified

The Supabase CLI v2.106.0 does not support `--project-ref` on `migration list`, and the local `.temp/linked-project.json` points to production. The staging project (`uekinikmjekqoevecvfi`) migration history could not be introspected without re-linking (a write operation, out of scope). It is unknown whether staging is in sync with the 17 local migrations or behind.

**Additional inconsistency:** The Supabase API reports `mygridtime-staging` as `linked: true`, but the local `.temp/linked-project.json` stores the production ref. These are in conflict.

### MEDIUM — `additional_redirect_urls` missing production domain

`supabase/config.toml` lists localhost and `127.0.0.1` as auth redirect URLs. The production domain (e.g. `https://app.mygridtime.com/auth/callback`) is not present. This is likely set in the Supabase dashboard rather than config.toml, but if the config were used to push settings it would overwrite the dashboard entry. Verify before any `supabase push` operation.

### LOW — `MGT_AI_EXTRACTION_ENABLED` template drift

`.env.local` contains `MGT_AI_EXTRACTION_ENABLED` but `.env.local.example` does not. The template is stale. If this variable is needed in Netlify build environments and not set there, AI extraction will be silently absent in deployed builds.

### LOW — `mygridtime-deploy` has no git history

The marketing site content at `C:\projects\mygridtime-deploy` is version-uncontrolled. Its Netlify site (`mygridtime`, `e974a27c`) is linked to a GitHub repo named `lights-out` — almost certainly a legacy or renamed repo. The actual source for the live marketing site may be in a repo not locally present.

### LOW — Supabase CLI version

Installed version is 2.106.0; 2.107.0 is available. Minor, but the older version is what blocked `--project-ref` on `migration list`.

### INFO — Commit message convention creates false alarm

The `[NOT YET APPLIED]` tag in commit `e6e6ebc` is now misleading since the migration has been applied. The commit history will permanently read as if the migration was never applied, creating confusion for future readers.

---

## GAPS IN THIS RECON

| # | Gap | What is needed to close it |
|---|---|---|
| G1 | Staging Supabase migration history unverified | Re-link to staging: `supabase link --project-ref uekinikmjekqoevecvfi` then `supabase migration list --linked`. Or use `--db-url` with staging DB password from Supabase dashboard. |
| G2 | Netlify per-context env vars (production vs preview vs branch) unverified | `cd C:\projects\mygridtime-app ; netlify link` then `netlify env:list` to audit all contexts. Confirms whether deploy previews use staging or production Supabase. |
| G3 | Which Netlify deployment is currently live on `app.mygridtime.com` — main or mgt-107-rehearsal? | Netlify dashboard → mygridtime-app site → Deploys tab. Check which branch/commit the active production deployment was built from. |
| G4 | Marketing site source repo | Identify the GitHub repo linked to Netlify site `e974a27c` (currently shows `lights-out`). May be a renamed repo. Check GitHub account for repos matching the content in `mygridtime-deploy`. |
| G5 | Staging Supabase link inconsistency | Run `supabase projects list` and compare `linked` field against `.temp/linked-project.json`. Run `supabase link --project-ref hxxderwxxpfzdxlmsqpl` to reconcile to production. |
| ~~G6~~ | ~~Sentry source maps in production~~ | **RESOLVED 2026-08-21.** `SENTRY_ORG` (`lb42-ltd`), `SENTRY_PROJECT` (`javascript-nextjs`) and `SENTRY_AUTH_TOKEN` (org token, secret) are set in the Netlify build env, scope Builds. The token is set in the **Production deploy context only**, so uploads run on `origin/main` builds and are correctly skipped on deploy previews / branch builds. `SENTRY_URL` deliberately unset — the org token embeds the `de.sentry.io` region. Verified on deploy `6a881e71` (commit `5092273`): Sentry debug IDs injected into deployed chunks, `.map` files 404 publicly. See DEC-043. |
| G7 | Production auth redirect URLs | Confirm `https://app.mygridtime.com/auth/callback` is in the Supabase production project's allowed redirect URLs (Supabase dashboard → Authentication → URL Configuration). |

---

## RECOMMENDED NEXT ACTIONS

**FIRST (highest priority):**

> **Verify which commit is deployed to production Netlify and confirm the MGT-107 migration/code alignment.**  
> Open the Netlify dashboard → mygridtime-app → Deploys. Check the live production deploy commit SHA. The production DB already has `championships` tables. If the deployed code also has championship references (MGT-104/105/106 on main) then the system is live and working. If the deployed code predates those commits, the app is broken in production right now.

---

2. **Merge `mgt-107-rehearsal` → `main`** once the above is confirmed safe. The branch adds only the migration commit and an empty rebuild trigger. No code conflicts expected.

3. **Fix `seed.sql`** — update all references from `organisations`/`org_id`/`org_invites` to `championships`/`championship_id`/`championship_invites` so that `supabase db reset` works locally after MGT-107.

4. **Create a staging `.env.local`** (e.g. `.env.staging.local`) pointing to the staging Supabase project so that local dev and destructive testing never touches the production database.

5. **Run `netlify link` in `mygridtime-app`** and then audit per-context env vars with `netlify env:list` to confirm production points to the production Supabase project and deploy previews point to staging.

6. **Add per-context env overrides to `netlify.toml`** (or document the dashboard-only approach explicitly in CLAUDE.md) so that the environment mapping is visible in code, not only in the Netlify UI.

7. **Verify staging migration parity** — re-link to staging (`supabase link --project-ref uekinikmjekqoevecvfi`) and run `supabase migration list --linked` to confirm all 17 migrations are applied, then re-link to production.

8. **Regenerate `database.types.ts`** via `supabase gen types typescript --linked > src/lib/types/database.types.ts` and switch the codebase to use generated types instead of the hand-maintained `src/lib/types/database.ts`. This prevents type drift as the schema evolves.

9. **Add `MGT_AI_EXTRACTION_ENABLED` to `.env.local.example`** to bring the template in sync with the active env file.

10. **Put `mygridtime-deploy` under git version control** — either create a dedicated repo or move the content into the existing `mygridtime-website` repo. The current state (hand-deployed, no history) is a maintenance risk.

---

*This document was generated from read-only recon on 2026-06-19. No files were edited, no git writes performed, no migrations run, no deploy commands executed.*
