# Project Status

## Stack

| Layer | Technology |
|-------|-----------|
| Framework | Next.js 14 (App Router) |
| Database | Supabase (PostgreSQL + Row Level Security) |
| Auth | Supabase Auth (magic links, OAuth) |
| Email | Resend (transactional) |
| Styling | Tailwind CSS 3 |
| Monitoring | Sentry (@sentry/nextjs) |
| Deployment | Netlify |
| Drag & Drop | @dnd-kit |
| Testing | Vitest |
| Language | TypeScript 5 |

## Architecture

- Server actions (`'use server'`) for all mutations — no REST API routes (except Stripe stub and dev-only `/api/auth/dev-session`)
- Supabase RLS enforces org-level access control via `get_user_org_role()`
- Multi-tenant: organisations → members → events → days → entries
- Soft deletes on events (`deleted_at`)
- Audit trail on all significant actions (`audit_log` table)
- Notification logging to `notification_log` table with 10-minute debounce
- Notification preferences in `notification_preferences` table (global per-email unsubscribe, token-based, service-role access only)

## Complete and Stable

- **Auth**: magic link login, OAuth callback, session management
- **Organisations**: create, switch, branding (logo, colour, header text)
- **Members**: invite (token-based, new invites fixed to `admin` role in the UI), accept, role management. Product-visible roles in `MemberManager` are reduced to `owner` + `admin`; legacy `editor` / `viewer` rows remain visible via a disabled `(legacy)` option and are not newly assignable. DB schema, RLS, `updateMemberRole()` server action, and `acceptInvite()` still accept all four role strings for backward compatibility (MGT-043).
- **Events**: full CRUD — create, edit, publish, unpublish, archive, duplicate
- **Timetable builder**: day management, entry CRUD, drag-drop reorder
- **Public view**: read-only timetable at `/{slug}`, print-friendly layout
- **Email notifications**: Resend integration, HTML + plain text templates, notification_log
- **Timetable save notifications**: opt-in via two explicit footer buttons in the timetable review modal — "Save only" (no notification) and "Save and notify" (triggers `sendEventNotification`). Surfaced only when the event is published and has at least one `notification_emails` recipient configured; otherwise the footer shows the existing single "Save" button. Replaces the earlier footer checkbox. The in-card "Accept & save" and "Undo skip & save" shortcuts always dispatch with `notify = false` — the explicit notify decision lives in the footer (MGT-045).
- **Publish notifications**: opt-in checkbox in publish dialog, default unchecked
- **Version history**: timetable snapshots created on each publish
- **Templates**: save event structure as reusable template, load into new events
- **Audit log**: tracks event lifecycle + day-structure + timetable changes with field-level diffs. Event lifecycle: `event.created`, `event.updated`, `event.published`, `event.unpublished`, `event.archived`, `event.duplicated`, `event.created_from_template`. Day structure: `event_day.added`, `event_day.removed`, `event_day.label_updated` (no-op label saves are suppressed so no entry is written when `from === to`). Timetable content: `timetable.updated` (add / remove / edit / reorder). Templates: `template.created`. Organisation scope (MGT-055): `organisation.created`, `organisation.updated`, `organisation.branding_updated`, `org_member.invited`, `org_member.invite_revoked`, `org_member.invite_accepted`, `org_member.role_updated`, `org_member.removed` — emitted from `src/app/admin/orgs/actions.ts` via the same `writeAuditLog()` helper, with `{ changes: { field: { from, to } } }` diff shapes matching the event-side conventions and no-op suppression on name / branding / role updates. Schema extended with nullable `audit_log.org_id uuid REFERENCES organisations(id)`; a `CHECK` constraint (`audit_log_scope_xor`) enforces that exactly one of `event_id` / `org_id` is set per row. `writeAuditLog()` now takes a discriminated-union `scope: { eventId: string } | { orgId: string }` argument. INSERT/SELECT RLS policies cover both branches (DEC-025). UI includes action-type filter dropdown, text search (action label, email, detail), date-range filtering (day-level inclusive), and CSV export of filtered entries. Auto-loads all entries on panel open (2000-row safety cap). Shared `writeAuditLog()` helper extracted to `src/lib/audit.ts`. Org-scoped rows are written to the database; the generalised reader now exists as `loadAuditLog(scope: AuditScope)` in `src/app/admin/audit/actions.ts` (MGT-056) — it accepts either `{ eventId }` or `{ orgId }`, preserves the 2000-row cap, and lets RLS (`audit_log_select_admin`) gate access for both branches. `loadAllAuditLog(eventId)` in `src/app/admin/events/actions.ts` is retained as a thin delegator so `AuditLogView` is unchanged; `AuditLogEntry` moved to `src/lib/audit.ts` alongside `AuditScope` and is re-exported from the events actions module for backwards-compat. `loadMoreAuditLog(eventId, cursor)` stays event-only (cursor-pagination fallback, not the primary flow). See DEC-026. Org-scoped rows are surfaced on `/admin/orgs/settings` via `OrgAuditLogView` (MGT-057), with live refresh wired from the interactive forms (MGT-058) and human-readable labels + CSV Summary column polished in MGT-059 / MGT-060 / MGT-062. Field-level verification complete (MGT-063 / MGT-065); dev-only verification fixture route added in MGT-064 and removed in MGT-066.
- **Notification log UI**: read-only **Notification history** panel on `/admin/events/{id}` below the audit log. Lists notification_log rows for the event — recipient email, type (`event.published` / `timetable.updated`), colour-coded status pill (`sent` / `failed` / `queued`), timestamp, and inline error on failed rows. Mirrors the audit-log shell (collapsible, initial 25-row server fetch, load-all-on-open with 2000-row cap, Sentry-tagged initial fetch + action, MGT-030-style Retry banner, parent-driven refresh on timetable save). Includes two client-side filter dropdowns — **Status** (all / sent / failed / queued) and **Type** (all / published / timetable updated) — a free-text search input (case-insensitive, matches recipient email, type label, status, and error text; composes with all other filters; MGT-048) — plus a **From / To** date-range filter (client-side, inclusive day-level, mirrors `AuditLogView`) all disabled while the initial load is in flight — and a **CSV export** of the currently-filtered set. A row of quick-filter chips sits above the dropdowns: **Failed**, **Sent**, **Published**, **Timetable updated**, **Today**, **Yesterday**, **Last 7 days**, **Last 30 days**, **This month**, **Last month** — each is a pure toggle over the existing filter state (status / type / date range), uses `aria-pressed` for active state, shares the `controlsDisabled` flag, and clicking an already-active chip clears the slice it set (MGT-049). **This month** sets `dateFrom` to the 1st of the current calendar month (local time) and `dateTo` to today, and aligns with calendar-month boundaries rather than the rolling 30-day window of **Last 30 days**. **Last month** sets `dateFrom` to the 1st of the previous calendar month and `dateTo` to its last day (both local time, via `new Date(y, m - 1, 1)` and `new Date(y, m, 0)`), giving a one-click view of the full preceding calendar month rather than the rolling 30-day window. A single **Clear filters** action appears on the date-range row (next to Export CSV) whenever any chip, dropdown, search, or date is active, and resets all five filter state pieces in one click — client-only, no new state. Above the MGT-049 chip row sits a **Presets** row of three one-click shortcuts — **Failures today**, **Sent today**, **Published today** — each of which writes into the existing filter state (setting `statusFilter` / `typeFilter` + both date inputs to today + clearing search) rather than introducing any new filter logic. Active state is derived by comparing the current state tuple to each preset's shape, so presets are mutually exclusive and remain correctly pressed/unpressed when the underlying state changes via any other control. Clicking an active preset clears all five filter state pieces (MGT-050). New server action `loadAllNotificationLog()` + exported `NotificationLogEntry` type in `src/app/admin/events/actions.ts`, new client component `src/components/admin/NotificationLogView.tsx`. No notification-system writes modified. MGT-053 consolidates the filter-state plumbing to a single `dateRangePresets: Record<AppliableDateRangeKey, { from, to }>` map that every mutation and derivation reads from — the nine `is{X}Active` booleans, the `dateRangeValue` ternary chain, and the `handleDateRangeChange` switch are all collapsed to one `.find()` derivation + a one-liner dispatch; `applyPreset` reads `dateRangePresets.today`; and the **Clear filters** button now invokes the existing `clearAllFilters()` helper rather than inlining the 5 setters. Zero user-visible behaviour change; no new state, no prop change, no backend touch — pure single-source-of-truth refactor inside `src/components/admin/NotificationLogView.tsx`.
- **Error boundaries**: `global-error.tsx`, root `error.tsx`, `(public)/error.tsx`, `admin/error.tsx`, `my/error.tsx` — styled error recovery UI across all route segments, all report to Sentry
- **Monitoring**: Sentry error tracking — client, server, and edge runtime coverage. Exceptions captured from all error boundaries and key server-side catch blocks. Conservative 10% trace sampling.
- **Environment validation**: Startup env var validation via `src/lib/env.ts`. Required vars (Supabase) error in all environments; server-required vars (service role key) error in production; feature-required vars (Resend) warn everywhere. Runs once in `instrumentation.ts` on nodejs runtime.
- **Consumer dashboard MVP** (`/my/*`): read-only consumer dashboard. Auth-guarded layout with org membership check. Timetable list (`/my`) shows published events across all user's orgs. Timetable view (`/my/{id}`) renders full timetable with day tabs, branding, and TimetableDay component. "Manage events" link shown only for elevated roles (owner/admin/editor). Shared `resolveEffectiveBranding()` utility extracted to `src/lib/utils/branding.ts`.
- **Notification preferences**: Global per-email unsubscribe via `notification_preferences` table. Token-based unsubscribe link in all event notification emails. Public unsubscribe page at `/notifications/unsubscribe/[token]` — no auth required. Unsubscribed recipients silently skipped during send. `List-Unsubscribe` header included. Admin event editor shows read-only unsubscribe state next to notification emails field (MGT-008 resolved).
- **Testing**: Vitest configured with `@` path alias. 57 smoke tests covering pure utility functions: app-url, slug, time, resend client, email templates (including unsubscribe links), env validation. No jsdom, no component tests, no Supabase mocking.
- **Dev tooling**: `/api/auth/dev-session` route creates a real Supabase session for `DEV_ADMIN_EMAIL` and redirects to `/admin`. Hard-gated on `NODE_ENV === 'development'` — returns 404 outside dev. Enables Claude Preview and local testing without magic-link email flow (DEC-010).
- **Template management UI (#11)**: Dedicated admin page at `/admin/templates` — server component listing all org templates with name, day count, and created date. Per-row "Use template" link navigates to `/admin/events/new?template={id}` with automatic preselection in `TemplatePicker`. Delete with confirmation dialog via client subcomponent (`TemplateActions`). Empty state guides users to save from event editor. Backend: `saveAsTemplate`, `listTemplates`, `deleteTemplate`, `createEventFromTemplate` server actions. New-event page reads `?template=` query param to preselect template mode.
- **Launch hardening (Tier 1)**: Viewport meta + manifest link in root layout. Security headers (X-Frame-Options, X-Content-Type-Options, Referrer-Policy) via `next.config.mjs`. `robots.txt` blocks `/admin`, `/my`, `/auth`, `/api`, `/invites`, `/notifications`. Dynamic `sitemap.ts` includes public routes and published event slugs. Loading skeletons for `/admin`, `/admin/events/[id]`, and `/my`. Privacy policy and terms of service at `/privacy` and `/terms`. Consumer stub pages (`/my/alerts`, `/my/drivers`, `/my/upload`) replaced with clean "Coming soon" UI — no internal phase language exposed.
- **Audit log UI (#10)**: action-type filter, text search (action label, user email, stringified detail — case-insensitive), date-range filtering (day-level inclusive), CSV export of filtered entries. Auto-loads all entries on panel open via `loadAllAuditLog()` server action (2000-row safety cap with UI warning if hit). No pagination — all filtering and export handled client-side.
- **Launch hardening (Tier 2)**: Browser `confirm()` replaced with `ConfirmDialog` in MemberManager. Empty-state copy standardised across all user-facing pages. Admin empty states improved in MemberManager (members + pending invites) and VersionHistory. Responsive fixes for event editor date/time grid, action bar, and invite form. Basic ARIA fixes: `aria-expanded` on collapsibles, `aria-label` on filter/role controls, `aria-pressed` on break/race toggle.
- **Bug & friction pass 1 (reliability hardening)**: Event mutation server actions now call `revalidatePath()` after successful writes so admin and public caches stay consistent without a manual reload. `writeAuditLog()` is contained — Supabase row errors and thrown exceptions are captured to Sentry and swallowed so audit-log failures can never flip a primary mutation's result. `duplicateEvent()` and `createEventFromTemplate()` detect day and entry insert failures, roll back the partially-created event via cascade delete, and return a clear error instead of silently returning success. `saveDayEntries()` detects failed inserts of new entries, returns an error, and skips audit logging and notifications on partial failure.
- **Bug & friction pass 2 (reliability + usability hardening)**: `EventEditor` "Save details" button is disabled (and shows "Saving…") while the metadata save is in flight, preventing concurrent submissions on both the direct-save and review-modal paths. `MemberManager` role changes now require confirmation via the existing `ConfirmDialog` — cancel relies on the controlled `<select value={member.role}>` to snap back to the current role on re-render, and downgrades get explicit "they will lose permissions" wording plus destructive styling. `MemberManager.loadData()` returns a structured result instead of swallowing errors; all call sites (invite/remove/role-change/revoke + `useEffect` refetch) surface a clear inline error if the refresh fails, and never fake success. `acceptInvite()` now error-checks the `org_invites.accepted_at` update on both branches (already-a-member and new insert) — a failed mark-as-accepted is reported to Sentry and returned as a clear failure instead of leaving a stale "pending" invite forever.
- **Bug & friction pass 4 (templates parity)**: Templates module brought up to the same reliability bar as events and members. `saveAsTemplate()`, `deleteTemplate()`, and `createEventFromTemplate()` in `src/app/admin/templates/actions.ts` now call a local `revalidateTemplatePaths()` helper (invalidates `/admin/templates` and `/admin/events/new`) on every success path; `createEventFromTemplate()` additionally revalidates `/admin/events`. Mirrors the MGT-009 pattern that was previously applied only to events. `TemplateActions.handleDelete()` in `src/app/admin/templates/TemplateActions.tsx` no longer swallows server failures — a new `deleteError` state keeps the `ConfirmDialog` open with an inline red error on failure, the row is left in place, and a `handleCancelDelete()` helper clears state on dismiss. Mirrors the MGT-014 pattern from MemberManager (MGT-021, MGT-022 resolved).
- **Bug & friction pass 5 (day-removal failure path)**: `removeEventDay()` in `src/app/admin/events/actions.ts` no longer swallows the `timetable_entries` delete error or leaks a raw Postgres `error.message` from the `event_days` delete. Both failure branches now capture to Sentry with distinct action tags (`removeEventDay.deleteEntries`, `removeEventDay.deleteDay`) and return a single generic user-facing message (`"Could not remove this day. Please retry."`). The entries-delete failure short-circuits before the parent delete, eliminating the orphan / FK-violation paths. Success-path revalidation is unchanged. `TimetableBuilder.handleRemoveDay()` surfaces the new error through its existing inline error UI without code changes (MGT-023 resolved).
- **Bug & friction pass 7 (orgs/actions.ts raw Postgres error leaks)**: Final pre-hardening gap in the admin server-action layer closed. Eleven failure paths across nine actions in `src/app/admin/orgs/actions.ts` (`createOrganisation` org+member inserts, `updateOrganisation`, `updateOrgBranding`, `listOrgMembers`, `updateMemberRole`, `removeMember`, `listOrgInvites`, `inviteMember` non-23505 + outer catch, `revokeInvite`, `acceptInvite` member insert) no longer return raw Supabase `error.message` strings to the admin UI. Each path now captures to Sentry with a distinct `action` tag (e.g. `createOrganisation.insertOrg`, `updateMemberRole.update`, `acceptInvite.insertMember`) and returns a single clean generic user-facing message (e.g. `"Could not update this member's role. Please retry."`). No control-flow, signature, or revalidation changes; existing client inline error UIs render the new messages without caller changes. Mirrors the MGT-023/024 pattern already applied to `events/actions.ts` day actions and brings the org admin surface to parity with events/templates (MGT-025 resolved).
- **Bug & friction pass 6 (day-add + day-label error masking + addEventDay public revalidation)**: `addEventDay()` and `updateDayLabel()` in `src/app/admin/events/actions.ts` no longer leak raw Postgres `error.message` through to the UI. Both functions now capture failures to Sentry with distinct action tags (`addEventDay.insert`, `addEventDay.insertNoData`, `updateDayLabel.update`) and return generic user-facing messages (`"Could not add this day. Please retry."` / `"Could not save this day label. Please retry."`). `addEventDay()` success path additionally calls `revalidatePublicEventPaths()` so adding a day to an already-published event no longer leaves `/{slug}`, `/{slug}/print`, `/my`, and `/` showing a stale day count. The three sibling day actions (add/remove/updateLabel) are now consistent with the MGT-023 pattern. No client changes required — `TimetableBuilder.handleAddDay()` and `handleSaveLabel()` already surface `result.error` through their existing inline error UIs (MGT-024 resolved).
- **Bug & friction pass 8 (events/actions.ts event-lifecycle hardening)**: Ten failure paths across seven event-lifecycle actions in `src/app/admin/events/actions.ts` (`createEvent` event + days inserts, `updateEventMetadata`, `publishEvent`, `unpublishEvent`, `archiveEvent`, `duplicateEvent` new-event insert + inner day/entries inserts, `saveDayEntries` delete + update/upsert paths) no longer return raw Supabase `error.message` (or `err?.message`) strings to the admin UI. Each path now captures the underlying error to Sentry with a distinct `action` sub-tag (`createEvent.insertEvent`, `createEvent.insertDays`, `updateEventMetadata.update`, `publishEvent.update`, `unpublishEvent.update`, `archiveEvent.update`, `duplicateEvent.insertEvent`, `duplicateEvent.insertDay`, `duplicateEvent.insertEntries`, `saveDayEntries.delete`, `saveDayEntries.update`) and returns a single clean generic user-facing message (`"Could not create/save/publish/unpublish/archive/duplicate this event. Please retry."` / `"Could not save entries. Please retry."`). `duplicateEvent()` rollback no longer interpolates raw child-error messages — `failureReason` is now a marker and the returned message is the generic duplicate error. No control-flow, signature, or revalidation changes; existing client inline error UIs render the new messages without caller changes. Mirrors the MGT-023/024/025 and Pass 7 pattern and closes the biggest remaining pre-hardening cluster in the admin server-action layer (MGT-026 resolved). Read-only helpers in the same file (`getVersionHistory`, `getSnapshotData`, `loadAllAuditLog`, `loadMoreAuditLog`) remain open for a future read-only-helper pass; `templates/actions.ts` mutating raw leaks are now closed by Pass 9 (MGT-027).
- **Bug & friction pass 9 (templates/actions.ts mutating raw-error hardening)**: The last mutating surface in the admin server-action layer still diverging from the MGT-023/024/025/026 pattern has been brought to parity. Six failure paths across three user-facing server actions in `src/app/admin/templates/actions.ts` (`saveAsTemplate` template insert, `deleteTemplate` delete, `createEventFromTemplate` event insert, `createEventFromTemplate` day insert inside the loop, `createEventFromTemplate` entries insert inside the loop, `createEventFromTemplate` rollback return) no longer leak raw Supabase `error.message` (or interpolated raw-error strings) to the admin UI. Each path now captures the underlying error to Sentry at the point of failure with a distinct `action` sub-tag (`saveAsTemplate.insert`, `deleteTemplate.delete`, `createEventFromTemplate.insertEvent`, `createEventFromTemplate.insertDay`, `createEventFromTemplate.insertEntries`) and returns a single clean generic user-facing message (`"Could not save this template. Please retry."`, `"Could not delete this template. Please retry."`, `"Could not create this event from template. Please retry."`). `createEventFromTemplate()`'s `failureReason` is now a `'day' | 'entry' | null` marker rather than a raw-error string; loop-level captures include `extra: { templateId, dayIndex }` so per-day failures remain diagnosable. The pre-existing synthetic outer `Sentry.captureException` at the rollback site has been removed — the point-of-failure captures replace it with better stack traces and sub-tags. Cascade-delete rollback on partial failure is unchanged. `listTemplates()` (read-only) still returns `error.message` and is deferred to a future read-only-helper pass alongside `events/actions.ts` read-only helpers. No client, control-flow, signature, or revalidation changes (MGT-027 resolved).
- **Bug & friction pass 11 (listTemplates raw-error hardening)**: The last remaining raw-leak in the admin server-action layer is closed. `listTemplates()` in `src/app/admin/templates/actions.ts` previously returned the raw Supabase `error.message` on select failure with no Sentry capture (explicitly deferred at the end of Pass 9 and Pass 10). It now captures the error to Sentry with `tags: { action: 'listTemplates.select' }` and returns the generic user-facing message `"Could not load templates. Please retry."`. No control-flow, signature, revalidation, or client changes; existing caller in `/admin/templates/page.tsx` still silently falls back to an empty list on failure — identical to the MGT-028 read-helper clients. A repo-wide `grep` for `error: error.message` across `src/` now returns zero results, confirming the admin server-action layer has no remaining raw-leak returns (MGT-029 resolved).
- **Bug & friction pass 10 (events/actions.ts read-only helper raw-error hardening)**: The four read-only helpers in `src/app/admin/events/actions.ts` (`getVersionHistory`, `getSnapshotData`, `loadAllAuditLog`, `loadMoreAuditLog`) no longer return raw Supabase `error.message` strings and no longer fail silently to Sentry. Each failure path now captures the underlying error with a distinct `action` sub-tag (`getVersionHistory.select`, `getSnapshotData.select`, `loadAllAuditLog.select`, `loadMoreAuditLog.select`) and returns a single clean generic user-facing message (`"Could not load version history. Please retry."`, `"Could not load this snapshot. Please retry."`, `"Could not load audit log. Please retry."`, `"Could not load more audit entries. Please retry."`). `getSnapshotData()`'s combined `error || !data` branch was split so the genuine "snapshot not found" case still returns the existing `"Snapshot not found."` message without touching Sentry. No client, control-flow, signature, or revalidation changes; `VersionHistory.handleView` and `AuditLogView.loadAll` already silently drop the error field, so the generic-message switch is invisible in the UI — purely adds Sentry observability and removes the raw-PG leak surface for any future consumer. Closes the last documented raw-error gap in `events/actions.ts`. `listTemplates()` in `templates/actions.ts` remains the last read-only helper still returning `error.message` and is deferred to a future read-only-helper pass (MGT-028 resolved).
- **Bug & friction pass 12 (AuditLogView silent-swallow + infinite retry loop)**: `AuditLogView.loadAll()` in `src/components/admin/AuditLogView.tsx` previously dropped `loadAllAuditLog()` failures on the floor. Because the adjacent `useEffect` guard is `(open && !allLoaded && !loadingAll)` and `setAllLoaded(true)` never fired on failure, every failed call was immediately retried as soon as the transition ended — an infinite retry loop that hammered Supabase, flooded Sentry (the server action is already hardened to capture every failure per MGT-028), and showed the user no error, just a flickering "Loading all entries..." banner. Fixed by adding a `loadError` state: `loadAll()` now clears it on entry and, on failure, sets the generic error message and `allLoaded = true` to stop the retry loop. A new inline red banner beneath the filter bar renders the error with a "Retry" button that clears the error and resets `allLoaded` so the existing effect picks it up. Success path, filter pipeline, CSV export, cap warning, and 2000-row safety behaviour are unchanged. Mirrors the MGT-014 / MGT-019 / MGT-022 pattern and additionally closes the retry-loop hazard (MGT-030 resolved). Pass-12 exploration also surfaced two deferred items — `listTemplates()` silent error in `/admin/events/new/page.tsx` and zero `writeAuditLog()` coverage across `orgs/actions.ts` — both explicitly out of scope for this pass.
- **First-org onboarding redirect (Pass A)**: `createOrganisation()` in `src/app/admin/orgs/actions.ts` now detects whether the current user already has any `org_members` rows before the new membership row is inserted, using the authenticated Supabase client (`org_members_select` RLS already lets users see their own rows — no admin-client widening needed). The result is threaded through the success payload as a new `isFirstOrg: boolean` field, so the return shape is now `ActionResult<{ id: string; isFirstOrg: boolean }>`. A failed probe is captured to Sentry with `tags: { action: 'createOrganisation.priorCount' }` and defaults `isFirstOrg` to `false` so a transient read error cannot misroute a returning user into the first-run path. `/admin/orgs/new` (`src/app/admin/orgs/new/page.tsx`) reads the flag and routes `router.push(result.data.isFirstOrg ? '/admin/orgs/settings' : '/admin')` — first-time owners land directly on the settings page where they can set branding before creating events, while subsequent org creates keep the existing `/admin` destination. Validation, slug-uniqueness probe, org + owner-membership inserts, cascade-delete rollback on membership-insert failure, outer/inner try/catch error handling (MGT-040), Sentry sub-tags at the insert sites, and error-message strings are all unchanged. No DB migration, no env-var changes, and no server-side redirect introduced — the navigation remains a client-side `router.push()` on `{ success: true }` (MGT-041 resolved).

- **Public organisation page (Pass B + C1)**: Public organisation route lists only published, non-deleted events for a single organisation. Pass C1 (2026-04-16) moved the canonical URL from `/o/{slug}` to `/{slug}`; `/o/{slug}` now 308-permanent-redirects to `/{slug}` for back-compat with previously shared URLs. Implemented as a server component in `src/app/(public)/o/[slug]/page.tsx`. Org row is resolved server-side via the admin Supabase client (anon RLS on `organisations` remains unchanged — same pattern already in use by the public landing and per-event pages). Unknown or errored slug resolution → `notFound()`. Event list query uses the standard anon client and applies `status = 'published' AND deleted_at IS NULL AND org_id = :id` (defence-in-depth over RLS). Event-list query failures are captured to Sentry with `tags: { action: 'publicOrgPage.listEvents' }` and the page degrades to the "No timetables have been published yet." empty-state render instead of erroring. Header renders org branding (`headerText`, `logoUrl`, `primaryColor`) identically to the public landing page, and the events table links each row to the existing per-event `/{slug}` URL (unchanged). `generateMetadata()` sets title + description from the resolved org. `export const dynamic = 'force-dynamic'` matches the rest of the public group. Settings surface: new `PublicOrgUrlField` client component in `src/app/admin/orgs/settings/PublicOrgUrlField.tsx` displays the full `{APP_URL}/o/{slug}` URL in a read-only input with a "Copy" button that mirrors the `EventEditor` public-URL clipboard pattern (`navigator.clipboard.writeText` + 2s "Copied!" confirmation). `src/app/admin/orgs/settings/page.tsx` now imports `getServerAppUrl` + `PublicOrgUrlField`, renders the field inside the existing Slug card, and replaces the previous "Public URLs are per-event, not per-organisation." help text with accurate copy pointing to the new `/o/{slug}` URL and noting that per-event URLs remain at `/{event-slug}`. Slug remains read-only — no edit behaviour changed. Sitemap (`src/app/sitemap.ts`) now additionally selects `org_id` on each published event, de-duplicates, and looks up `organisations.slug` via the admin client; `/o/{slug}` entries are emitted with `changeFrequency: 'weekly'` / `priority: 0.6` only for orgs with at least one published event. Admin-client failure inside the sitemap degrades to event-only routes, matching the existing fallback shape. No DB migration, no RLS changes, no role/permission changes, no changes to Pass A (MGT-042 resolved).

- **Bug & friction pass 21 (createOrganisation false error on post-commit side effects)**: `createOrganisation()` in `src/app/admin/orgs/actions.ts` now wraps its entire body in an outer try/catch and wraps the two post-commit side effects (`setActiveOrgId(org.id)` and `revalidatePath('/admin')`) in an inner try/catch. Previously, any exception thrown after the organisation + owner-membership inserts had already committed (for example a cookie-store write failure from `setActiveOrgId()` or a runtime failure inside `revalidatePath()`) propagated out of the server action and either crashed the `/admin/orgs/new` route or flipped the user-facing result from success to error — even though the DB write had landed. The form then showed a generic error and the user, believing the action had failed, retried and hit the now-taken slug path, creating the appearance of a broken create-org flow. The outer try/catch captures unexpected exceptions to Sentry with `tags: { action: 'createOrganisation' }` and returns a safe `ActionResult` (`"Could not create the organisation. Please retry."`). The inner try/catch captures post-commit exceptions to Sentry with `tags: { action: 'createOrganisation.postCommit' }` and swallows them so the caller still sees `{ success: true, data: { id } }` when the DB write has committed — cache-revalidation or cookie-write failures no longer turn a successful org creation into a false-failure in the UI. Existing validation (name/slug trimming and length checks), slug-uniqueness probe, organisation insert, owner-membership insert, cascade-delete rollback on membership-insert failure, sub-tag Sentry captures at the insert sites (`createOrganisation.insertOrg`, `createOrganisation.insertMember`), error-message strings, and the `ActionResult<{ id: string }>` return shape are all unchanged. No client-behaviour changes — `/admin/orgs/new` continues to navigate on `{ success: true }` via its existing `router.push()`; no server-side redirect was introduced. Mirrors the outer-catch pattern already established in `inviteMember()` (MGT-025) and closes the last known false-error path on the org creation flow (MGT-040 resolved).

- **Bug & friction pass 20 (EventEditorPage timetable_snapshots initial-query silent-swallow, no rescue path)**: Pass 19's note that the `timetable_snapshots` initial-list silent-swallow remained the next-ranked deferred sibling (queued as MGT-038) is now closed. `EventEditorPage` at `src/app/admin/events/[id]/page.tsx:90` previously destructured only `{ data: snapshotRows }` and dropped `error` — on failure `snapshotRows ?? []` collapsed to `[]`, `versions` became `[]`, and `<VersionHistory versions={[]} />` hit its early-return rendering `"No versions yet. A snapshot is saved each time you publish."`, indistinguishable from a genuinely empty publish history. The claimed MGT-032 rescue (`VersionHistory.handleView()`) only fires on per-click `getSnapshotData()` — with no rows rendered there is nothing to click, so the rescue path was unreachable. Zero Sentry signal at the query site. Fixed by additionally capturing `snapshotsError`, reporting to Sentry with `tags: { action: 'eventEditorPage.listSnapshots' }` (following the MGT-023 → MGT-037 sub-tag pattern), computing a generic `versionsLoadError` string (`"Could not load version history. Please retry."`), and threading it through `EventEditor` as a new optional `versionsLoadError` prop. `EventEditor` imports `ERROR_BANNER` from `@/lib/styles` (extending the existing import) and renders a section-scoped `ERROR_BANNER` (`role="alert"`) immediately above `<VersionHistory />` when the prop is set. No `VersionHistory` changes — its own `loadError` state (MGT-032) continues to rescue per-click view failures. Page reload is the retry path for the initial list, consistent with MGT-033 / MGT-034 / MGT-035 server-component precedent. Mirrors the MGT-014 / MGT-019 / MGT-022 / MGT-030 / MGT-031 / MGT-032 / MGT-033 / MGT-034 / MGT-035 / MGT-036 / MGT-037 silent-swallow remediation chain and closes the last documented same-shape silent-swallow on the event editor page (MGT-038 resolved). `users` publisher-email query (cosmetic) and `orgs/actions.ts` zero `writeAuditLog()` coverage (observability gap) remain explicitly deferred.
- **Bug & friction pass 19 (EventEditorPage audit_log initial-query silent-swallow, no rescue path)**: Pass 18's note that `audit_log` initial-query failures were "rescued by MGT-030 on panel open" was invalidated during Pass 19 exploration. `AuditLogView` (`src/components/admin/AuditLogView.tsx:249`) seeds `allLoaded` from `!initialHasMore`, and when the server-side `audit_log` query fails, `auditRows` falls back to `[]`, collapsing `auditHasMore` to `false` and flipping `allLoaded` to `true` at mount — the panel-open `useEffect` guard `(open && !allLoaded && !loadingAll)` never fires, `loadAllAuditLog()` never runs, and MGT-030's retry path is short-circuited. Users saw "No audit entries yet." with no retry affordance, and zero Sentry signal reached monitoring. Fixed by additionally capturing `auditError` in `src/app/admin/events/[id]/page.tsx`, reporting to Sentry with `tags: { action: 'eventEditorPage.listAudit' }` (following the MGT-023 → MGT-036 sub-tag pattern), computing a generic `auditLoadError` string (`"Could not load audit log. Please retry."`), and threading it through `EventEditor` as `auditLoadError` and into `AuditLogView` as a new optional `initialLoadError` prop. `AuditLogView` now seeds its `loadError` state from `initialLoadError ?? null` so the existing Retry banner (lines 413–427) renders on panel open, and seeds `allLoaded` from `!initialLoadError && !initialHasMore` so the existing `useEffect` naturally fires `loadAll()` on panel open as a retry path — either clearing the banner on success or updating it with the hardened generic message on continued failure. No new dependencies, no new banner UI, no test-surface changes; existing retry logic is reused verbatim. Mirrors the MGT-014 / MGT-019 / MGT-022 / MGT-030 / MGT-031 / MGT-032 / MGT-033 / MGT-034 / MGT-035 / MGT-036 silent-swallow remediation chain and closes the last HIGH-impact silent-swallow with no rescue on the event editor page (MGT-037 resolved). `timetable_snapshots` initial-list silent-swallow remains the next-ranked deferred candidate (same shape; MGT-032 only rescues per-click views; queued as MGT-038). `users` publisher emails (cosmetic) and `orgs/actions.ts` zero `writeAuditLog()` coverage (observability gap) remain explicitly deferred.
- **Bug & friction pass 18 (EventEditorPage silent days/entries swallow)**: `EventEditorPage` (`src/app/admin/events/[id]/page.tsx`) previously destructured only `{ data: days }` and `{ data: entries }` from the two highest-impact queries on the editor — a silent failure on either left `EventEditor` hydrated with `dayList = []` / `entries = []`, so the editor rendered as if the event had zero days or a wiped timetable. Since `createEvent()` always inserts at least one day, an "empty" event editor is an anomaly, not a legitimate state. Fixed by additionally capturing `daysError` / `entriesError`, capturing each failure to Sentry with a distinct `action` sub-tag (`eventEditorPage.listDays`, `eventEditorPage.listEntries`) per the MGT-023 → MGT-029 pattern, and computing a combined `loadError` string (`"Could not load this event. Please retry."` when both fail; single-line variants when only one fails). An inline `ERROR_BANNER` (`role="alert"`) renders immediately below the breadcrumb and above `<EventEditor …/>`. `ERROR_BANNER` added to the existing `@/lib/styles` import; `Sentry` added as a new `@sentry/nextjs` namespace import. The existing `?? []` fallbacks are unchanged — the editor still hydrates on the error path so the header, breadcrumb, metadata, and action bar remain usable; a page reload re-executes the queries. The three sibling silent queries on the same page — `timetable_snapshots` (MEDIUM, rescued by MGT-032 per-view banner), `users` for publisher emails (LOW, purely cosmetic), and `audit_log` (MEDIUM, rescued by MGT-030 on panel open) — are explicitly deferred: their rescue paths already mitigate user visibility, and touching them would broaden scope beyond the safest subset. Mirrors the MGT-014 / MGT-019 / MGT-022 / MGT-030 / MGT-031 / MGT-032 / MGT-033 / MGT-034 / MGT-035 silent-swallow remediation chain and closes the last HIGH-impact silent-swallow in the admin surface (MGT-036 resolved). `orgs/actions.ts` zero `writeAuditLog()` coverage remains an observability gap and is deferred.
- **Bug & friction pass 17 (OrgSettingsPage silent members/invites fallback)**: `OrgSettingsPage` (`src/app/admin/orgs/settings/page.tsx`) previously collapsed both hardened results with `const initialMembers = membersResult.success ? membersResult.data : []` (and the matching invites line), dropping `error` entirely. On any `listOrgMembers()` / `listOrgInvites()` failure, `MemberManager` hydrated with empty arrays on first paint and the L11 empty-state copy ("No members yet." / "No pending invites.") rendered as if the org were genuinely empty. `MemberManager`'s first-mount `useEffect` refetch is guarded by `isMounted`, so there was no client-side rescue. Most dangerously: an owner viewing their own settings page saw "no members" — impossible in a healthy state, since they are always at least a member themselves — making a transient PG error, RLS regression, or schema drift look like a team-roster wipe with no retry path. Fixed by additionally capturing `membersError` / `invitesError` and computing a combined `loadError` string (single-message fallthrough when one fails, `·`-joined when both fail), importing `ERROR_BANNER` from `@/lib/styles`, and rendering an inline `ERROR_BANNER` (`role="alert"`) inside the **Members & invites** section immediately above `<MemberManager …/>` when `loadError` is non-null. The banner is intentionally section-scoped — the org name / slug / branding sections above it loaded from the `organisations` table and remain fully functional. The existing `[]` fallbacks for `initialMembers` / `initialInvites` are unchanged; `MemberManager`'s internal `loadData()` on mutation remains the live retry path. No Sentry capture at the page layer — `listOrgMembers.select` / `listOrgInvites.select` already capture at the server-action layer (MGT-025). No client refactor, no retry button, no redirect; stays a pure server component and a reload re-executes the actions. Mirrors the MGT-014 / MGT-019 / MGT-022 / MGT-030 / MGT-031 / MGT-032 / MGT-033 / MGT-034 silent-swallow remediation chain and closes the last documented server-component silent-swallow in the admin surface (MGT-035 resolved). The `EventEditorPage` five-query silent-swallow cluster (days, entries, snapshots, publisher emails, audit rows) remains the last deferred candidate — HIGH impact but multi-query refactor scope, explicitly deferred beyond Pass 17. `orgs/actions.ts` zero `writeAuditLog()` coverage remains an observability gap (not a user-facing bug) and is also deferred.
- **Bug & friction pass 16 (Admin dashboard silent event-query error)**: `AdminDashboardPage` (`src/app/admin/page.tsx`) previously destructured the top-level events query with `const { data: events } = await eventsQuery`, dropping `error` entirely. The empty-state branch at line 85 (`!events || events.length === 0`) then rendered "No events found." with a "Create your first event →" CTA, making any transient PG error, RLS regression, or schema drift indistinguishable from a genuinely empty workspace — highest blast radius of any remaining silent-swallow because the dashboard is the first page every admin sees after login, and the empty-state CTA nudged users toward **creating duplicate events** believing their workspace was empty. Fixed by destructuring `{ data: events, error: eventsError }`, capturing failures to Sentry with `tags: { action: 'adminDashboard.listEvents' }`, computing a `loadError` string (`"Could not load events. Please retry."`), and rendering an inline `ERROR_BANNER` (`role="alert"`) immediately above the status filter tabs — visible regardless of active filter. Existing empty-state branch is unchanged — a genuine zero-event result still shows "No events found." with its CTAs; a failed load additionally surfaces the hardened generic message above. No control-flow, filter, or rendering changes; stays a pure server component. Mirrors the MGT-014 / MGT-019 / MGT-022 / MGT-030 / MGT-031 / MGT-032 / MGT-033 silent-swallow remediation pattern and closes the highest-impact remaining visibility gap in the admin landing surface (MGT-034 resolved). Pass-16 exploration confirmed two sibling deferred candidates — `OrgSettingsPage` silent `[]` fallback on `listOrgMembers()` / `listOrgInvites()` failure (MEDIUM impact, pure server-component banner fix) and `EventEditorPage` five-query silent-swallow cluster (HIGH impact but broader scope) — both explicitly out of scope for this pass.
- **Bug & friction pass 15 (TemplatesPage silent-swallow)**: `TemplatesPage` (`src/app/admin/templates/page.tsx`) previously collapsed the hardened `listTemplates()` result with `const templates = result.success ? result.data : []`, so any transient PG error, RLS regression, or schema drift silently hid every template in the org and rendered the "No templates yet." empty state — indistinguishable from a genuine zero-template org. Fixed by additionally capturing `const loadError = result.success ? null : result.error` and rendering an inline `ERROR_BANNER` (`role="alert"`) above the list / empty-state block when the load fails. `ERROR_BANNER` added to the existing `@/lib/styles` import. The empty-state branch is unchanged — a genuine empty result still shows "No templates yet."; a failed load additionally surfaces the hardened generic message (`"Could not load templates. Please retry."`) above it. No retry button / client split needed — the component stays a pure server component and a reload re-executes the server action. Mirrors the MGT-014 / MGT-019 / MGT-022 / MGT-030 / MGT-031 / MGT-032 silent-swallow remediation pattern and closes the last documented deferred silent-swallow candidate in the admin surface (MGT-033 resolved). `orgs/actions.ts` zero `writeAuditLog()` coverage remains an observability gap, not a user-facing bug, and is explicitly deferred beyond Pass 15.
- **Bug & friction pass 14 (VersionHistory snapshot-load silent-swallow)**: `VersionHistory.handleView()` in `src/components/admin/VersionHistory.tsx` previously dropped `getSnapshotData()` failures on the floor. The hardened server action (MGT-028) already returned a clean generic message and captured to Sentry with `tags: { action: 'getSnapshotData.select' }`, and also distinguished a genuine `"Snapshot not found."` branch, but the client only branched on `result.success === true` — failure set `setLoading(false)` and did nothing else, so the user clicked "View", saw the button briefly grey out, and then observed zero UI response: no modal, no banner, no retry path. Fixed by adding `loadError` and `lastAttemptedSnapshotId` state. `handleView()` now clears `loadError` on entry, records the snapshot id as a retry anchor, wraps the `await` in `try…finally` so `loading` is always released, populates `viewingSnapshot` and clears the retry anchor on success, and stores `result.error` on failure (fallback `"Could not load this snapshot. Please retry."`) without opening the modal. New `handleRetry()` re-invokes `handleView(lastAttemptedSnapshotId)`; new `handleDismissError()` clears both fields. A new inline red banner (`role="alert"` / `aria-live="polite"`) renders inside the expanded panel above the version list with "Retry" (disabled while `loading` or when the retry anchor is null) and "Dismiss" buttons. Both the `getSnapshotData.select` infrastructure-failure branch and the `"Snapshot not found."` branch are now surfaced through the same banner. Success path, collapsible header, version list, per-row `disabled={loading}` guard, modal, day-tab state, `TimetableDay` render, and empty state are unchanged. Mirrors the MGT-014 / MGT-019 / MGT-022 / MGT-030 / MGT-031 pattern (MGT-032 resolved). Two sibling candidates remain explicitly deferred for Pass 15: `TemplatesPage` server-component `[]` fallback on `listTemplates()` failure (cosmetic, requires server→client refactor) and `orgs/actions.ts` zero `writeAuditLog()` coverage (observability gap, multi-action scope).
- **Bug & friction pass 13 (NewEventPage listTemplates silent-swallow)**: `NewEventPage` (`src/app/admin/events/new/page.tsx`) previously dropped `listTemplates()` failures on the floor. The `useEffect` only branched on `result.success`, so any server-side failure (already hardened per MGT-029: generic message + Sentry capture) resulted in `templates = []`, `templatesLoaded = true`, the "Use template" tab hidden entirely, and — most importantly — any `?template={id}` preselection coming from `/admin/templates` silently dropped. An admin who clicked "Use template" could unknowingly submit a **blank** event instead of one built from the template they had just picked, with no error, no hint, and no retry path. Fixed by extracting `loadTemplates` into a `useCallback` and adding a `templatesError` state: on failure the error message is stored, `templates` is reset to `[]` defensively, and `templatesLoaded` is set to `true` so no retry loop can form. A new inline red `ERROR_BANNER` renders above the mode toggle with a "Retry" button and, when `?template=` was requested, an extra line explaining that the preselected template could not be loaded. The "Create event" submit button is additionally disabled whenever `preselectedTemplate && templatesError` so the accidental-blank-event path is no longer reachable without a conscious cancel/restart. Success path, form fields, submission flow, and all other behaviour are unchanged (MGT-031 resolved). Pass-13 exploration also confirmed two sibling deferred candidates — `VersionHistory.handleView()` dropping `getSnapshotData()` errors and `TemplatesPage` (server component) falling back to `[]` on `listTemplates()` failure — both explicitly out of scope for this pass.
- **Phase A — platform access compatibility shortcut (backend + RLS only)**: Introduces a lightweight platform-staff access path so platform operators can reach any customer org for support and recovery work, without building a full platform-admin role tier. Schema migration `supabase/migrations/20260416000000_phase_a_platform_role.sql` adds `users.platform_role text CHECK (… IN ('staff','support'))` and `audit_log.actor_context jsonb`. A new `is_platform_staff()` SQL helper (`SECURITY DEFINER STABLE`) checks `users.platform_role IS NOT NULL` for the current `auth.uid()`. The existing `get_user_org_role(p_org_id)` helper is extended to short-circuit to `'owner'` when `is_platform_staff()` returns true, so every policy that already went through `get_user_org_role()` inherits platform access with no policy-text changes. The five SELECT policies that bypass that helper with direct `org_id IN (SELECT … FROM org_members …)` subqueries (`orgs_select_members`, `org_members_select`, `events_select_members`, `event_days_select_members`, `entries_select_members`) are extended with `OR is_platform_staff()`. Application layer: `Database['public']['Tables']['users']` and `Database['public']['Tables']['audit_log']` types in `src/lib/types/database.ts` gain `platform_role` and `actor_context`; new `PlatformRole` and `ActorContext` exports expose the discriminated shape. `getActiveOrg()` in `src/lib/utils/active-org.ts` is rewritten with a platform branch — reads `users.platform_role`, then prefers the cookie-selected org, falls back to any real membership, and finally to the oldest organisation in the system, always returning `role: 'owner', via: 'platform', platform_role: <role>`. The normal membership branch now returns `via: 'membership'`. A new pure `makeActorContext(source)` helper in `src/lib/audit.ts` derives the audit-log actor payload from an `ActiveOrg`-like shape (`via: 'platform'` → `{ via, platform_role }`; `via: 'membership'` → `{ via }`), and `writeAuditLog()` gains an optional `actorContext` parameter that writes to `audit_log.actor_context`. All 11 `writeAuditLog()` call sites across `src/app/admin/events/actions.ts` (9 sites: `event.created`, two `event.updated`, two `event.published`, `event.unpublished`, `event.archived`, `event.duplicated`, `timetable.updated`) and `src/app/admin/templates/actions.ts` (2 sites: `template.created`, `event.created_from_template`) now pass `makeActorContext(membership)`. Plumbing on the read path: `AuditLogEntry`, the two internal `AuditRowRaw` types, both mappings in `loadAllAuditLog()` / `loadMoreAuditLog()`, and the `EventEditorPage` mapping all thread the new `actor_context` column through so every selected audit row matches the widened `AuditLog` row type. A new `src/lib/audit.test.ts` adds 5 test cases for `makeActorContext` (membership baseline; membership with stray `platform_role` ignored; platform+staff; platform+support; platform with missing role normalised to `null`). Editors retain publish capability. Org creation remains open to any authenticated user. Platform staff are cross-org and do not require org membership. The `via` discriminator is preserved through `ActiveOrg` and `audit_log.actor_context` so platform-reached access is never presented as genuine customer ownership in UI copy, audit reporting, or downstream tooling. No UI changes in Phase A — consuming the `actor_context` field in the audit-log viewer is deferred (DEC-018).
- **Bug & friction pass 3 (onboarding + silent-failure hardening)**: `/auth/callback` distinguishes zero-memberships from viewer-only and routes each to a useful destination — zero memberships → `/admin/orgs/new` (first-run onboarding), viewer-only → `/my`, elevated → `/admin`. Middleware sets an `x-pathname` request header so `admin/layout.tsx` can permit `/admin/orgs/new` through its guard for users with zero memberships, and shows a "Welcome — create your first organisation" CTA instead of "Access denied" when a zero-membership user lands on `/admin`. `AcceptInviteForm` renders a manual "Continue to …" link alongside the post-accept auto-redirect so users are not stranded if the client-side navigation stalls. `getDatesInRange()` no longer silently caps at 14 days — `MAX_EVENT_DAYS` and a new `countDaysInRange()` helper are exported from `slug.ts`, and `createEvent()`, `createEventFromTemplate()`, and `duplicateEvent()` all pre-validate the user-supplied range and return a clear error (`"Events are limited to 14 days. The selected range spans N days — please shorten it."`) instead of silently dropping days. `TimetableBuilder.handleSaveLabel()` inspects the `updateDayLabel()` result, shows the error inline in a red banner, keeps the tab in edit mode on failure, and guards against double-invocation via a `savingLabel` flag. `sendEventNotification()` captures errors on every `notification_preferences` upsert (and on the follow-up fetch), reports failures to Sentry, refuses to send to any recipient without a readable preference row (no unsubscribe link = no send), and records a `notification_log` row with `status: 'failed'` for each skipped recipient so the failure is visible.

## In Progress

- **MGT-086 (partial — one follow-up open)**: forward-fix landed and docs reconciled; stale wording drift closed in MGT-088 (2026-04-22). Remaining gap: MGT-087 (behavioural editor verification outstanding — no editor row on the linked remote, no mutation permitted). Tracked in KNOWN_ISSUES.md. No production impact; verification debt only.

## Recent Changes

- **Stale "owners and admins" wording replaced (2026-04-22, MGT-088)**: Closes the cosmetic drift deferred from MGT-086. Eight error strings in [src/app/admin/orgs/actions.ts](src/app/admin/orgs/actions.ts) rewrote `"Only owners and admins can <verb> …"` → `"Only owners can <verb> …"` at lines 217 (`update organisation settings`), 272 (`update branding`), 349 (`view members`), 385 (`change roles`), 453 (`remove members`), 527 (`view invites`), 554 (`invite members`), 667 (`revoke invites`). Enforcement logic unchanged — `requireOwner()` still short-circuits on `activeOrg.role !== 'owner'` (DEC-037). Scope is wider than the MGT-086 note originally called out: a pre-commit repo-wide grep surfaced all 8 sibling occurrences, per the KNOWN_ISSUES instruction to "grep for any sibling strings with the same drift before committing". Repo-wide grep for `owners and admins` across `src/` and `scripts/` now returns zero hits (only historical doc references remain in KNOWN_ISSUES / PROJECT_STATUS). `tsc --noEmit` clean; vitest 79/79 green; `next build` clean. Deploy impact: **code: yes (1 file, 8 lines), db: no, env: no, manual verify: deferred to MGT-087 (copy renders on the same non-owner guard paths that the editor verification exercises)**.

- **Reconcile shipped MGT-084 state & repair broken MGT-085 HEAD (2026-04-22, MGT-086 — partial)**: Forward-fix landed and doc reconciliation landed, but the ticket is **not fully complete**: behavioural editor verification is still outstanding (no editor row exists on the linked remote and the plan forbade mutating remote data to create one — tracked as **MGT-087** in KNOWN_ISSUES.md) and a stale wording drift in [src/app/admin/orgs/actions.ts:217](src/app/admin/orgs/actions.ts) still reads "owners and admins" despite MGT-084's collapse to owner-only enforcement (tracked as **MGT-088** in KNOWN_ISSUES.md). What did land: forward-fix for commit `2723db1` which shipped unresolved merge-conflict markers in [src/app/layout.tsx](src/app/layout.tsx). Working tree already contained the correct manual resolution using `APP_VERSION` from [src/lib/version.ts](src/lib/version.ts); this ticket stages that resolution and commits it as a forward-fix (no amend, no force-push). Audits the shipped state of MGT-084: 8 read-only remote Supabase queries via `supabase db query --linked` against `hxxderwxxpfzdxlmsqpl` confirmed both migrations present (`20260420010000_mgt_084_roles`, `20260420010001_mgt_084_rls_cleanup`); `org_members.role` CHECK constraint allows only `owner` and `editor`; zero surviving `admin` or `viewer` rows in `org_members`; `org_invites.role` CHECK allows only `editor`; `users.platform_role` allows `admin | staff | support | null`; `users.subscription_status` exists allowing `member | subscriber`; at least one rewritten policy body matches DEC-037 (`IN ('owner','editor')`); no surviving `'admin'` or `'viewer'` org-role literals in any `pg_policy` body. Code-level permission helpers verified from source: [requireOwner()](src/app/admin/orgs/actions.ts) enforces `activeOrg.role === 'owner'` (note: cosmetic drift in the error string at line 217 still reads "Only owners and admins can update organisation settings." — logic is correct, wording left for a cosmetic-copy follow-up); [requireEditor()](src/app/admin/events/actions.ts) admits `'owner' | 'editor'` only; [src/app/auth/callback/route.ts](src/app/auth/callback/route.ts) routes zero-membership users to `/admin/orgs/new` and elevated roles to `/admin`. Behavioural verification done in `npm run dev` (fresh server after `.next` wipe): `/admin/orgs/settings` loaded correctly, MemberManager rendered members + pending-invites panels, invite form has **no** role picker (hard-coded to `editor`), header badge rendered "Staff — {OrgName}" per DEC-037, org switcher A↔B fully refreshed the settings page, footer badge displayed `v0.1.1` on `/admin`, `/my`, and public `/mgt-060-verify-org/mgt-082-qa-duplicate-test`, zero console errors across all routes. Owner flow verified against the existing owner account. Editor behavioural flow **not exercised** — `select user_id, org_id, role from public.org_members where role = 'editor'` returned zero rows on the linked remote, and per plan the gate required "no unsafe remote mutation"; this gap is explicitly reported rather than papered over with a synthetic editor insert. Docs reconciled: MGT-085 paragraph updated from `v0.1.0` to `v0.1.1` to match `package.json.version = "0.1.1"` (package version left as-is per user decision), KNOWN_ISSUES MGT-086 entry added marking the broken `2723db1` HEAD resolved same-day via forward-fix. `tsc --noEmit` clean; vitest green; `next build` clean. Deploy impact: **code: yes (1 file — resolved `layout.tsx`), db: no, env: no, manual verify: done (owner flow + UI smoke across admin/my/public with badge visible)**.

- **Footer version badge (2026-04-20, MGT-085)**: Surfaces the running app version in the UI for QA/support. New [src/lib/version.ts](src/lib/version.ts) re-exports `APP_VERSION` from `package.json.version` (no new version constant — `package.json` remains the single source of truth; `resolveJsonModule` already enabled in [tsconfig.json](tsconfig.json)). [src/app/layout.tsx](src/app/layout.tsx) imports `APP_VERSION` and renders a small fixed badge (`v{APP_VERSION}`) as a sibling after `{children}` inside `<body>` — `fixed bottom-1.5 right-2 z-50 select-none pointer-events-none rounded px-1.5 py-0.5 text-[10px] font-mono text-gray-500/70 bg-white/60 backdrop-blur-sm`. Root layout is the narrowest shell that guarantees app-wide visibility, so the badge appears on every route group (`(public)`, `admin`, `my`, `auth`, `invites`, `notifications`) without per-group edits. `pointer-events-none` + `z-50` ensures it never blocks clicks and stays below modals (which use `z-[60]+`). No env var, no DB change, no migration, no new dependency, no new style token. `tsc --noEmit` clean; vitest 79/79 green; `next build` clean (21 static pages generated). Browser-verified on the dev server at `/admin` — computed style `position: fixed`, `bottom: 6px`, `right: 8px`, `z-index: 50`, `pointer-events: none`; span text reads `v0.1.1`; `aria-label="App version 0.1.1"` present in SSR HTML for `/admin`, `/my`, and `/` (public root); zero console errors. Deploy impact: **code: yes (2 files + 1 docs line), db: no, env: no, manual verify: done (bottom-right badge visible across admin + public + `/my`)**.

- **Roles & permissions foundation + header role badge (2026-04-20, MGT-084)**: Aligns the role model to the intended 3-axis shape and surfaces it in the header. **PLATFORM** axis ([users.platform_role](supabase/migrations/20260420010000_mgt_084_roles.sql)) extends to `admin | staff | support | null` (new `admin` value); **ORG** axis ([org_members.role](supabase/migrations/20260420010000_mgt_084_roles.sql)) collapses from `owner|admin|editor|viewer` to `owner | editor` — legacy `admin` rows rewritten to `editor`, legacy `viewer` rows deleted (they retain auth via the new subscription axis and reach `/my`); **SUBSCRIPTION** axis (new [users.subscription_status](supabase/migrations/20260420010000_mgt_084_roles.sql) column, `member | subscriber`, default `member`). `org_invites.role` narrows to `editor` only. Two sequential migrations: [20260420010000_mgt_084_roles.sql](supabase/migrations/20260420010000_mgt_084_roles.sql) (data rewrite + CHECK constraint tightening + new column) and [20260420010001_mgt_084_rls_cleanup.sql](supabase/migrations/20260420010001_mgt_084_rls_cleanup.sql) (19 policies rewritten across `organisations`, `org_members`, `org_invites`, `events`, `event_days`, `timetable_entries`, `audit_log`, `notification_log`, `ai_extraction_log`, `templates`, `timetable_snapshots`, `storage.objects event-extractions` — `IN ('owner','admin')` → `= 'owner'`, `IN ('owner','admin','editor')` → `IN ('owner','editor')`; policy NAMES preserved as historical labels). `is_platform_staff()` OR-clauses on SELECT policies preserved byte-for-byte so DEC-018 cross-org platform access is unchanged; `get_user_org_role()` short-circuits for `admin`/`staff`/`support` unchanged (all three return effective `'owner'`). **Badge priority**: platform `admin` → **Admin** (no org qualifier — global) > platform `staff`/`support` → **Staff — {OrgName}** / **Support — {OrgName}** > org membership → **Owner — {OrgName}** / **Editor — {OrgName}** > orgless → **Subscriber** / **Member**. The UI **never substitutes Owner for Staff/Support** — `ActiveOrg.via: 'platform' | 'membership'` drives the label so platform access is honestly represented. TypeScript narrowed: `OrgMember.role` → `'owner' | 'editor'`; `AppUser.platform_role` → `'admin' | 'staff' | 'support' | null`; new required `AppUser.subscription_status: 'member' | 'subscriber'`; `OrgInvite.role: 'editor'`. New discriminated union [UserBadge](src/lib/types/roles.ts); new pure helper [computeUserBadge()](src/lib/utils/role-badge.ts) branches on `platform_role='admin'` first, then `activeOrg.via`, then subscription fallback. Permission helpers: [requireOwnerOrAdmin()](src/app/admin/orgs/actions.ts) renamed to `requireOwner()` with single allowed role `'owner'` (platform staff/admin still pass via the `get_user_org_role()` shortcut); [requireEditor()](src/app/admin/events/actions.ts) tightened with explicit `role IN ('owner','editor')` check (previously permitted any membership — latent bug, no behaviour change post-collapse). New client component [UserMenu.tsx](src/components/admin/UserMenu.tsx) renders the badge pill + popover (name, email, Role, Subscription, switchable org list, Sign out); wired into [admin/layout.tsx](src/app/admin/layout.tsx) and [my/layout.tsx](src/app/my/layout.tsx), replacing the inline email+logout chrome. `/my` layout no longer gates on membership — orgless subscribers/members reach the page and see the empty-state. `MemberManager.tsx` loses the role picker on invite (editor is the only assignable role). `/auth/callback/route.ts` simplified — `['owner','editor']` only; the zero-memberships → `/admin/orgs/new` first-run branch is preserved. Invite acceptance routes straight to `/admin` (no viewer branch). Settings surface gated on `activeOrg.role === 'owner'` (was `['owner','admin']`). **Trade-off (documented)**: existing `org_members.role = 'admin'` rows are rewritten to `'editor'` — current admins lose org-settings access unless the owner re-promotes them. Pre-deploy audit query documented in [DECISIONS.md](docs/DECISIONS.md) DEC-037. `tsc --noEmit` clean; vitest 79/79 green; `next build` clean. Migrations applied to the linked pre-launch remote `hxxderwxxpfzdxlmsqpl` via `supabase db push` on 2026-04-20 (one NOTICE on `notification_log_insert_editor` — policy did not yet exist on remote, DROP-IF-EXISTS + CREATE handled gracefully). Deploy impact: **code: yes (types, 2 helpers, 2 layouts, 1 new component, 1 renamed helper, invite flow narrowing), db: yes (2 migrations applied), env: no, manual verify: required (5-flow browser check — owner badge, platform admin/staff/support relabel, orgless subscriber, invited editor editor-gate, DB role-count verification)**.

- **Org switcher refreshes Organisation settings page (2026-04-20, MGT-083)**: Fixes a dual-org bug where switching active org via the top-bar `<OrgSelector>` updated the `/admin` layout but left `/admin/orgs/settings` showing the previous org's name, slug, branding, members, invites, extraction log, and audit log until a manual browser refresh. Root cause was client-state preservation: the page RSC ([src/app/admin/orgs/settings/page.tsx](src/app/admin/orgs/settings/page.tsx)) correctly re-ran on `router.refresh()` and passed fresh props, but child client forms (`OrgNameForm`, `BrandingForm`) seed local `useState` from props only at mount — React kept the prior org's values because component identity was unchanged. Fix: added `key={org.id}` to `<SettingsPanels>` so a change in active org fully unmounts and remounts the client subtree, re-seeding every `useState` from the new props. Contributing weakness in [src/app/admin/orgs/actions.ts](src/app/admin/orgs/actions.ts) `switchOrg()` widened from `revalidatePath('/admin')` to `revalidatePath('/admin', 'layout')` so the entire `/admin` layout subtree is invalidated on switch (belt-and-braces alongside `router.refresh()`). No other `revalidatePath('/admin')` call sites were touched — individual mutations keep their narrower scope. Known tradeoff (acceptable): an unsaved edit in `OrgNameForm`/`BrandingForm` is discarded when the user switches org mid-edit; this is the correct UX — stale draft values must not bleed across orgs. `tsc --noEmit` clean; vitest 79/79 green; `next build` clean (settings bundle 11.7 kB unchanged). Dual-org browser verification completed on the dev server 2026-04-20: seeded a second organisation `MGT QA Org B` (slug `mgt-qa-org-b`, primary colour `#0066FF`, logo `mgt-qa-org-b-logo.png`, header text `Bravo HQ`) owned by the dev admin via the new standalone seed script [scripts/seed-second-org.mjs](scripts/seed-second-org.mjs) (npm script `seed:mgt-083` in [package.json](package.json)) — idempotent find-or-create against `organisations` + `org_members`, logs every step, fails loudly on missing env, prints DELETE SQL for cleanup. Visibly distinct branding vs Org A `MGT-060 Verify Org` (colour `#FF0000`, logo `logo-v3.png`, no header text). Loaded `/admin/orgs/settings` as Org A then dispatched a native `change` event on the top-bar `<OrgSelector>` to switch to Org B; every field updated without manual browser refresh — `h1`, `OrgNameForm` name input, public URL (slug text), `BrandingForm` colour + logo URL + header text. Repeated the switch three full A↔B cycles — each cycle restored the correct org's values with no cross-bleed; zero console errors and zero console warnings across the full run. Docs: KNOWN_ISSUES MGT-083 marked RESOLVED with verification log; no new DEC (standard React `key`-based remount idiom). Deploy impact: **code: yes (2 lines + 1 dev-only seed script + 1 package.json script line), db: no, env: no, manual verify: done (dual-org owner)**.

- **Per-org event slugs + nested public event URL (2026-04-20, MGT-082)**: Fixes a production bug where creating an event whose title matched an existing event in *any* organisation failed with `duplicate key value violates unique constraint "events_slug_key"`. New migration [supabase/migrations/20260420000000_events_slug_org_scoped.sql](supabase/migrations/20260420000000_events_slug_org_scoped.sql) drops the global `UNIQUE(slug)` on `events` and replaces it with composite `UNIQUE(org_id, slug)`, so slug uniqueness is now scoped per organisation. Canonical public event URL changes from `/{eventSlug}` to `/{orgSlug}/{eventSlug}` — new routes [src/app/(public)/[slug]/[eventSlug]/page.tsx](src/app/(public)/[slug]/[eventSlug]/page.tsx) and [src/app/(public)/[slug]/[eventSlug]/print/page.tsx](src/app/(public)/[slug]/[eventSlug]/print/page.tsx) resolve the event by `(org_id, slug)` via the existing `resolvePublicOrgBySlug` helper. The org landing route [src/app/(public)/[slug]/page.tsx](src/app/(public)/[slug]/page.tsx) is rewritten org-first; when the slug does not match an organisation it falls back to a legacy-event lookup and — only when **exactly one** published event across all orgs carries that slug — issues a `308` permanent redirect to the nested canonical URL via `permanentRedirect`. Ambiguous legacy matches 404 rather than silently picking one. The legacy `/[slug]/print` route [src/app/(public)/[slug]/print/page.tsx](src/app/(public)/[slug]/print/page.tsx) is reduced to the same 308-redirect shape. Server actions [src/app/admin/events/actions.ts](src/app/admin/events/actions.ts) (`createEvent`, `duplicateEvent`) and [src/app/admin/templates/actions.ts](src/app/admin/templates/actions.ts) (`createEventFromTemplate`) replace the old `generateUniqueSlug` loop with a new `computeEventSlug(supabase, orgId, title)` helper that pre-checks `(org_id, slug)` uniqueness and returns a user-facing error — **"An event with this title already exists in this organisation. Please choose a different title."** — on collision (no silent auto-suffix; unexpected DB errors still routed to Sentry). `revalidatePublicEventPaths()` now invalidates both `/[orgSlug]/[eventSlug]` and the legacy `/[slug]` route. Link generators threaded `orgSlug` through every canonical path: [src/app/(public)/page.tsx](src/app/(public)/page.tsx) landing page, [src/components/public/PublicOrgView.tsx](src/components/public/PublicOrgView.tsx), [src/app/my/[timetableId]/page.tsx](src/app/my/[timetableId]/page.tsx) ("View public page" button), [src/components/admin/EventEditor.tsx](src/components/admin/EventEditor.tsx) (URL preview + admin copy-link), [src/app/admin/events/[id]/page.tsx](src/app/admin/events/[id]/page.tsx) (resolves and passes `orgSlug` to the editor), [src/app/sitemap.ts](src/app/sitemap.ts), and [src/lib/resend/notifications.ts](src/lib/resend/notifications.ts) (email `publicUrl`). All use `organisations!inner(slug)` joins or an admin-client lookup because anon RLS does not grant SELECT on `organisations`. Reserved-slug guard in `createOrganisation` is unchanged (org slug still must not collide with any published event slug globally, so legacy `/{eventSlug}` redirects remain stable). `updateEventMetadata` intentionally does not regenerate the event slug — slugs are stable after creation. Docs: new DEC-036 captures the nested-canonical-URL + no-auto-suffix + 308-legacy-redirect rationale; DEC-022 status updated to "Partially superseded by DEC-036 (2026-04-20)"; KNOWN_ISSUES MGT-082 entry added. Migration `20260420000000_events_slug_org_scoped.sql` applied to the linked pre-launch remote `hxxderwxxpfzdxlmsqpl` via `supabase db push` on 2026-04-20; service-role probe confirmed `events_org_id_slug_key` UNIQUE is enforced on `(org_id, slug)` and the legacy global `events_slug_key` is gone (cross-org insert with matching slug accepted, same-org insert rejected with the new constraint name in the error). Browser verified on the restarted dev server (`.next` dev-cache wiped): create→editor preview nested URL; same-org duplicate title → ERROR_BANNER with exact spec copy; canonical `/{orgSlug}/{eventSlug}` → 200; legacy `/{eventSlug}` → 308 to nested; `/{orgSlug}/{eventSlug}/print` → 200; `/{orgSlug}` → 200; sitemap + landing + `PublicOrgView` + `EventEditor`-view-public-page all emit nested URLs only; no console errors. Deploy impact: **code: yes, db: yes (migration now applied to the pre-launch remote; any additional environments must still run `supabase db push`), env: no, manual verify: yes**.

- **Extraction storage retention cron (2026-04-20, MGT-081)**: Closes the deferred 30-day cleanup for the `event-extractions` Storage bucket and matching `ai_extraction_log` rows. New shared helper [src/lib/retention/extractions.ts](src/lib/retention/extractions.ts) — `runExtractionRetention({ admin, olderThanDays = 30, now })` — pages through old rows in batches of 1000, chunks storage removals in groups of 100, removes storage objects **first**, then deletes only the DB rows whose objects were confirmed removed (or whose `source_path` is null/empty). Partial storage failures do not throw; failing paths are left for the next run and surfaced in `storageErrors`. Idempotent and safe to run repeatedly; a no-op on empty state. New authenticated GET route [src/app/api/cron/retention-extractions/route.ts](src/app/api/cron/retention-extractions/route.ts) (`dynamic = 'force-dynamic'`, `runtime = 'nodejs'`) bearer-auths against `CRON_SECRET` — 401 on mismatch, 503 when the secret is not configured, JSON `{ rowsDeleted, objectsRemoved, storageErrors }` summary on success. New manual runner [scripts/retention-extractions.mjs](scripts/retention-extractions.mjs) wired as `npm run retention:extractions` fetches the same route so both entry points exercise identical production code. `CRON_SECRET` added to [src/lib/env.ts](src/lib/env.ts) as `feature-required`. New test suite [src/lib/retention/extractions.test.ts](src/lib/retention/extractions.test.ts) — 6 tests covering empty state, old-row deletion, young-row preservation, null `source_path`, storage-error retry, and idempotence. `tsc --noEmit` clean; vitest 79/79 green; `next build` clean (new route registered as `ƒ /api/cron/retention-extractions`). No schema change, no RLS change, no UI, no new runtime dependency. Scheduler-platform wiring (Vercel Cron / Netlify Scheduled / GitHub Actions) intentionally deferred to a follow-up. Docs: new DEC-035; KNOWN_ISSUES / MGT-069 deferred-items list updated to reflect closure; new QA runbook added to [docs/QA_RUNBOOKS.md](docs/QA_RUNBOOKS.md).

- **Extraction log — click-to-filter from failure breakdown (2026-04-20, MGT-080)**: Makes the MGT-079 **Top failures** chips interactive on [src/components/admin/ExtractionLogView.tsx](src/components/admin/ExtractionLogView.tsx). Each breakdown chip is now a `<button>` with `aria-pressed` state; clicking a chip applies the corresponding error code as a filter (search input set to the code, `chipFilter` set to `'all-problems'` to narrow to all non-success rows) so the row list collapses to just the failures carrying that code. Only one breakdown chip can be active at a time — clicking a different chip replaces the active filter; clicking the already-active chip clears it (resets `searchQuery`, `chipFilter`, `statusFilter`). Active-state styling reuses the existing MGT-077 red triage-chip palette (`bg-red-50 text-red-700 border-red-300`) — no new design tokens. Minimal new local state (`activeBreakdownCode: string | null`) tracks which chip is active; `clearFilters()` resets it, `applyChip()` (for the `Problems today` / `All problems` presets) resets it, and the search input's `onChange` deactivates it when the user types a value that no longer matches the active code. Metrics row and breakdown chip counts themselves remain derived from `allEntries` and unaffected by any filter — they still answer "system health" rather than "what matches my view" (MGT-078 / MGT-079 contract preserved). The `"unknown"` bucket chip writes the literal string `"unknown"` into the search input per spec — real `rate_limited` rows carry `error_code = null`, so the filter matches zero rows for that chip against the current seed; this is the documented edge case. No server query added, no new server action, no schema change, no RLS change, no new env var, no new runtime dependency, no change to `loadExtractionLog()` or any other action. `tsc --noEmit` clean; vitest 73/73 green; `next build` clean (settings bundle 11.6 kB → 11.7 kB). Browser-verified on `/admin/orgs/settings` against the MGT-075 seed pair — clicking `claude_call_failed (1)` narrows the 6-row seed to the single Error row with that code while metrics stay at 6/6/3/3 and the breakdown row still shows all three chips; switching to `schema_mismatch (1)` replaces the active filter and narrows to the Validation failed row; typing into the search deactivates the active chip; clicking the active chip a second time clears the filter back to 6 rows; **Clear filters** resets chip + search + `chipFilter` together; no console errors. Presentation-only extension of DEC-032 / DEC-033 — no new DEC.

- **Extraction log failure breakdown row (2026-04-20, MGT-079)**: Adds a compact, client-derived failure-breakdown row to [src/components/admin/ExtractionLogView.tsx](src/components/admin/ExtractionLogView.tsx), placed directly below the MGT-078 metrics row and above the filter bar. Extends the existing `metrics` `useMemo` (no second loop) to build a `Map<error_code, count>` over `allEntries` where `status !== 'success'`, bucketing `error_code === null | undefined` as `"unknown"`, then emits a `failureBreakdown` of the top 3 codes by count (descending). The row renders one neutral border-gray chip per code in the shape `<code> (<count>)` with a TOP FAILURES label prefix, reusing the same `border-gray-200 bg-white text-gray-600` chip shape already used by the filter toolbar — no icons, no tooltips, no hover behaviour, no colour emphasis. Section is fully hidden when there are zero failures (`failureBreakdown.length === 0`). Like the metrics row it derives from `allEntries` only, so it intentionally does NOT react to `statusFilter` / `chipFilter` / `dateFrom` / `dateTo` / `searchQuery` — it answers "what is failing across all loaded rows?" rather than "what matches my filter view?". No server query added, no new server action, no schema change, no RLS change, no new env var, no new runtime dependency, no change to `loadExtractionLog()` or any other action. `tsc --noEmit` clean; vitest 73/73 green; `next build` clean (settings bundle 11.4 kB → 11.6 kB). Browser-verified on `/admin/orgs/settings` against the MGT-075 seed pair — post-seed the row shows `claude_call_failed (1) unknown (1) schema_mismatch (1)` (rate_limited row's null error_code correctly bucketed as `unknown`); applying `status = error` narrows visible rows to 1 while the breakdown row remains unchanged (three chips still shown); post-cleanup the row is hidden entirely (empty state); no console errors. Presentation-only extension of DEC-032 / DEC-033 — no new DEC.

- **Extraction log lightweight metrics row (2026-04-20, MGT-078)**: Adds a compact client-derived metrics row to [src/components/admin/ExtractionLogView.tsx](src/components/admin/ExtractionLogView.tsx), placed directly below the panel header and above the filter bar. Four small stat blocks — **Attempts** (total loaded rows), **Today** (rows whose `created_at` matches the local calendar date via the existing file-local `toLocalIsoDate()` helper, per DEC-024), **Succeeded** (`status === 'success'`), **Failed** (all other statuses: `error` + `rate_limited` + `validation_failed`) — with a muted `↑ N%` failure-rate line rendered under **Failed** whenever `attempts > 0`. Metrics are derived from `allEntries` (the full 2000-cap loaded payload) via a single `useMemo`, so they intentionally do NOT react to `statusFilter` / `chipFilter` / `dateFrom` / `dateTo` / `searchQuery` — metrics answer "system health" rather than "what matches my filter view". The existing 2000-row cap banner copy ("Showing first 2,000 entries. …") is refined to "Showing first 2,000 entries. Metrics reflect these rows only." so the cap implication on the new stats is explicit without adding a second banner. Metrics render in both empty and non-empty states (all zeros with the failure-rate line hidden when `attempts === 0`). No server query added, no new server action, no schema change, no RLS change, no new env var, no new runtime dependency, no change to `loadExtractionLog()` or any other action. `tsc --noEmit` clean; vitest 73/73 green; `next build` clean (settings bundle 11.1 kB → 11.4 kB). Browser-verified on `/admin/orgs/settings` against the MGT-075 seed pair — post-seed the row shows Attempts 6 / Today 6 / Succeeded 3 / Failed 3 with ↑ 50%; applying `status = error` narrows visible rows to 1 while the metrics block remains unchanged (6 / 6 / 3 / 3 / 50%); post-cleanup the empty state shows 0 / 0 / 0 / 0 with the failure-rate line hidden; no console errors. Presentation-only extension of DEC-032 / DEC-033 — no new DEC.

- **Extraction log triage chips + sort (2026-04-20, MGT-077)**: Adds two quick-triage chips to [src/components/admin/ExtractionLogView.tsx](src/components/admin/ExtractionLogView.tsx) — **Problems today** (narrows to non-`success` rows created today, local-calendar matched via the file-local `toLocalIsoDate()` helper per DEC-024) and **All problems** (narrows to every non-`success` row regardless of date) — plus a minimal **Sort order** `<select>` (Newest first / Oldest first). Chips mirror the `NotificationLogView` idiom: `aria-pressed` reflects active state, clicking the active chip again clears it, and activating a chip first clears `statusFilter` / `dateFrom` / `dateTo` / `searchQuery` so the chip is the sole filter expression. A dedicated `chipFilter` state handles "all non-`success`" because the existing status dropdown can only express a single enum value at a time; the existing status / date / search filters remain fully composable on top of an active chip. The filter toolbar now also shows a **Clear filters** button whenever any filter or chip is active (mirroring NLV), and `clearFilters()` now resets `chipFilter` **and** `sortOrder` back to `'newest'` alongside the four original filter pieces. Sort is a post-filter `[...result].reverse()` on the already-`DESC`-sorted server payload — no new query, no fetch, no dep on `refreshSignal` semantics (DEC-027 unchanged). No schema change, no RLS change, no new API, no env-var touch, no new runtime dependency. `tsc --noEmit` clean; vitest 73/73 green; `next build` clean (settings bundle grew 10.1 kB → 11.1 kB, in line with MGT-074). Browser-verified end-to-end on `/admin/orgs/settings` against the MGT-075 seed pair (`npm run seed:extractions` / `npm run cleanup:extractions`): sort `newest ↔ oldest` inverts row order; **Problems today** narrows the 6-row seed to the three non-`success` rows (Error / Rate limited / Validation failed) and clears back to 6; **All problems** matches the same three rows (all seeded rows are today, so the behavioural difference between the two chips is only observable across multi-day data); **Clear filters** resets chip + sort + all filter inputs. No new DEC — the change is a presentation-only extension of DEC-032 / DEC-033 and re-uses the NotificationLogView chip idiom wholesale.

- **QA runbooks — Extraction log dev verification (docs-only, 2026-04-20, MGT-076)**: Adds a new top-level operational-docs file [docs/QA_RUNBOOKS.md](docs/QA_RUNBOOKS.md) and an **Extraction log — dev verification** section covering the MGT-075 seed/cleanup flow from end to end: prerequisites, required env vars inline (`NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `DEV_ADMIN_EMAIL`), explicit "non-production only" warning, the `npm run seed:extractions` command with a per-row expected-state table, browser verification steps against `/admin/orgs/settings`, the `npm run cleanup:extractions` command, and a troubleshooting table for the common first-run failures. The runbook references DEC-033 for rationale rather than duplicating it, so design decisions remain single-sourced. [docs/README.md](docs/README.md) adds the new file to the index. [docs/LAUNCH_PLAN.md](docs/LAUNCH_PLAN.md) cross-links the dev-verification harness line (#13) to the runbook. New DEC-034 codifies `docs/QA_RUNBOOKS.md` as the canonical home for operational verification flows tied to dev-only tooling and draws the line between runbooks (how to run), decisions (why it exists), and status (what is built). No code, script, UI, schema, RLS, migration, server-action, or env-var change — docs-only.

- **Extraction log dev seed + cleanup scripts (2026-04-20, MGT-075)**: Local/dev-only verification harness for the ExtractionLogView surface (MGT-073 / MGT-074). New [scripts/seed-extraction-log.mjs](scripts/seed-extraction-log.mjs) uses the service-role client to insert 6 rows into `ai_extraction_log` for the org where `DEV_ADMIN_EMAIL` is a member — covering every rendered branch: success with a live `event_id` link, success unlinked, success linked to a soft-deleted event (falls back to unlinked when no soft-deleted event exists in the org), `error` (error_code `claude_call_failed`, null tokens), `rate_limited` (no model, no error_code), `validation_failed` (error_code `schema_mismatch`). Rows are marked via `source_path = 'seed/mgt-075/<uuid>'` so the companion [scripts/cleanup-extraction-log.mjs](scripts/cleanup-extraction-log.mjs) deletes them deterministically via `LIKE 'seed/mgt-075/%'`. Wired in [package.json](package.json) as `npm run seed:extractions` / `npm run cleanup:extractions` — both use `node --env-file=.env.local` (Node 20+) so no new dev deps. No production runtime surface introduced: no new API route, no migration, no RLS change, no UI change, no env-var addition. Verified end-to-end on `/admin/orgs/settings` — seed produces the expected 6-entry panel (Success × 3 with token counts + linked event, Error with error_code, Rate limited, Validation failed with error_code); cleanup zeros the panel ("0 entries", collapsed empty state). Covers the MGT-071-BLOCKED gap where real extractions cannot run without `ANTHROPIC_API_KEY` but the log UI still needs exercising. `tsc --noEmit` clean; vitest 73/73 green; `next build` clean. Docs: new DEC-033.

- **Extraction log UX polish (2026-04-20, MGT-074)**: Presentation-only polish pass over [src/components/admin/ExtractionLogView.tsx](src/components/admin/ExtractionLogView.tsx). (1) Empty state redesigned from a single grey line to a centred block with a neutral document icon, heading ("No extraction attempts recorded.") and sub-line ("Real AI extractions appear here. Mock runs are not logged."); filtered-but-empty variant now shows a one-click **Clear filters** button that resets all four filter state pieces (statusFilter / searchQuery / dateFrom / dateTo). (2) Dismissible blue info banner shown only when the raw log is empty: "Tip: only real extractions (AI enabled) are logged. Mock runs are skipped by design." — dismiss is React state only, no localStorage. (3) Status pills now render an inline `bg-current` dot before the label (shape reinforces colour for red-green a11y), carry a `title={statusLabels[status]}` tooltip, and `validation_failed` text darkened to `text-orange-800` for clearer separation from `rate_limited` amber. (4) New `lastUpdatedAt` state initialised at mount to `new Date().toISOString()` when the SSR fetch did not error (covers the common empty-log case where `allLoaded` starts `true` and `loadAll()` never runs), and re-set on every subsequent successful `loadAll()` resolution; rendered as "Updated · {formatTimestamp(...)}" inside the filter bar when entries exist, or as a right-aligned line directly under the panel header when the list is empty. Reuses the existing `formatTimestamp()` helper — no new date util. No query changes, no schema, no API, no new deps, no dependency on `refreshSignal` semantics (DEC-027 unchanged). `tsc --noEmit` clean; vitest 73/73 green; `next build` clean. DEC-032 addendum notes the polish pass; no new decision introduced.

- **Extraction audit visibility — admin log surface (2026-04-20, MGT-073)**: Adds a read-only `Extraction log` section to `/admin/orgs/settings` so admins can inspect AI extraction outcomes (success / error / rate_limited / validation_failed), spot failure clusters, and trace a created event back to the original upload. New `loadExtractionLog(orgId)` server action in [src/app/admin/extractions/actions.ts](src/app/admin/extractions/actions.ts) queries `ai_extraction_log` with `users:user_id ( email )` and `events:event_id ( id, title, slug, deleted_at )` joins, orders by `created_at DESC`, and caps at 2000 rows — mirroring the shape of `loadAuditLog()` (DEC-026). RLS (`ai_extraction_log_select_members`, MGT-070) remains the sole access gate. New client component [src/components/admin/ExtractionLogView.tsx](src/components/admin/ExtractionLogView.tsx) is a copy-shape of `OrgAuditLogView`: collapsible panel, client-side filters (status dropdown, date range From/To, free-text search across email / error_code / model / event title), status pill per row (`success` green, `error` red, `rate_limited` amber, `validation_failed` orange), model + token counts on success rows, raw `error_code` on non-success rows, and a link to the created event (`(event deleted)` fallback when the joined `events.deleted_at` is non-null). `refreshSignal` is wired to the existing `SettingsPanels` `bumpRefresh` (DEC-027) so the panel re-fetches after a member/branding save without a full page reload. The section lives between `Members & invites` and `Audit log`; visibility inherits the owner/admin page gate at `src/app/admin/orgs/settings/page.tsx:27`. Mock-path extractions (`MGT_AI_EXTRACTION_ENABLED=false`) are invisible here by design — the mock path at `src/app/admin/events/extract/actions.ts:160` returns without writing an `ai_extraction_log` row, and the empty-state copy documents this. No CSV export, no `was_modified` column (defer — flag remains visible in per-event audit row per DEC-031), no new route, no new nav link. **No DB migration, no new env vars, no RLS change, no new runtime dependency.** Vitest 73/73 green; `tsc --noEmit` clean; `next build` clean (settings bundle 10.1 kB). Browser-verified on `/admin/orgs/settings` — new section renders, panel opens with documented empty-state copy, no console errors. New DEC-032 covers the "nest under settings" and "defer was_modified" choices.

- **Extraction review & correction UI — contract formalised (2026-04-20, MGT-072)**: The "From PDF / image" preview step is now an explicit **review-and-correct** gate before persistence. `ExtractionPreview` ([src/components/admin/ExtractionPreview.tsx](src/components/admin/ExtractionPreview.tsx)) was already fully editable; MGT-072 closes the contract by (1) capturing the server-returned `ExtractedEvent` once into a `useRef` pristine snapshot so edits never mutate the reference, (2) computing `wasModified` at confirm via `JSON.stringify(pristineRef.current) !== JSON.stringify(state)` and passing it through a widened `onConfirm(edited, wasModified)` signature, (3) threading the flag into `ExtractionMeta.was_modified?: boolean` on the call to `saveExtractedEventContent` in [src/app/admin/events/new/page.tsx](src/app/admin/events/new/page.tsx), and (4) updating the intro microcopy to "Review and edit — you can change any field before creating." `saveExtractedEventContent` ([src/app/admin/events/extract/actions.ts](src/app/admin/events/extract/actions.ts)) continues to accept the full user-edited payload with no re-extraction and no new API call; the audit row carries `was_modified` via the existing `meta` object with zero DB schema changes. Works identically in mock mode and AI-disabled mode — review logic is client-side and has no `ANTHROPIC_API_KEY` dependency. MGT-071 gate (`NEXT_PUBLIC_AI_EXTRACTION_READY`) untouched. Verified by typecheck, `next build`, and 73/73 vitest pass; `/admin/events/new` renders cleanly with the gate banner intact (no regression). No migrations, no env changes, no new routes.

- **AI-assisted event extraction — blocked in env pending API key (2026-04-17, MGT-071)**: Feature shipped but not enabled in this environment. Migrations `20260418000000_add_ai_extraction_log.sql` and `20260418000001_add_event_extractions_bucket.sql` applied to the linked remote Supabase project `hxxderwxxpfzdxlmsqpl` via `supabase db push`. `MGT_AI_EXTRACTION_ENABLED=true` is set in `.env.local`, but `ANTHROPIC_API_KEY` has not been populated yet, so the `/admin/events/new` → **From PDF / image** tab renders with its file input `disabled` and an inline `ERROR_BANNER` ("AI extraction not configured yet. This feature will be enabled once API access is set up."). Readiness is derived at Next boot via `NEXT_PUBLIC_AI_EXTRACTION_READY` in [next.config.mjs](next.config.mjs) from the real `ANTHROPIC_API_KEY` presence (`(process.env.ANTHROPIC_API_KEY?.trim() ?? '') !== '' ? 'true' : 'false'`) — no secret leaks to the client bundle, only the derived boolean string. Server behaviour unchanged (flag + rate-limit + log-row paths intact); with flag on and key missing the existing try/catch at [src/app/admin/events/extract/actions.ts:258-278](src/app/admin/events/extract/actions.ts) still writes `ai_extraction_log { status: 'error', error_code: 'claude_call_failed' }` and returns the generic user-facing error, but the UI gate makes this unreachable in the normal flow. Full enablement unblocks once `ANTHROPIC_API_KEY` is set and the Next process is restarted — see MGT-071-BLOCKED in KNOWN_ISSUES for the unblock path. No extraction-code changes, no migration changes, no mocks added.

- **AI-assisted event extraction — Phase B real Claude Vision integration (2026-04-17, MGT-070)**: Replaces the MGT-069 mock response with a guarded real Claude Vision call. New env vars in [src/lib/env.ts](src/lib/env.ts): `ANTHROPIC_API_KEY` (`feature-required`), `MGT_AI_EXTRACTION_ENABLED` (`optional`, default `false`), `MGT_EXTRACT_MODEL` (`optional`, defaults to `claude-sonnet-4-6`). New dep `@anthropic-ai/sdk` in `package.json`. Two new migrations: `20260418000000_add_ai_extraction_log.sql` creates `public.ai_extraction_log` (id, org_id, user_id, event_id, source_mime, source_bytes, source_path, model, tokens_input, tokens_output, status CHECK in ('success','error','rate_limited','validation_failed'), error_code, created_at) with RLS restricting SELECT to `get_user_org_role() IN ('owner','admin','editor')` and no authenticated INSERT policy (service-role writes only); `20260418000001_add_event_extractions_bucket.sql` creates the private `event-extractions` Storage bucket with editor+ INSERT and member SELECT policies scoped to the first path segment (`{org_id}/{extraction_id}/{ts}.{ext}`). New `extractWithClaude({bytes, mime})` in [src/lib/ai/extract.ts](src/lib/ai/extract.ts) calls `client.messages.create` with a prompt-cached system prompt, forces tool use via `tool_choice: { type: 'tool', name: 'emit_event' }` against a JSON schema mirroring `ExtractedEvent`, parses the `tool_use` block, runs the existing `isExtractedEvent()` guard (throws `ExtractValidationError` on mismatch), and returns `{ event, model, tokens_input, tokens_output }`. PDFs go through as `type: 'document'` + base64; PNG/JPEG as `type: 'image'` + base64. `extractEventFromUpload()` in [src/app/admin/events/extract/actions.ts](src/app/admin/events/extract/actions.ts) now runs the full guarded path: MIME/size/empty validation (unchanged) → feature-flag check (off → 800ms mock as before) → 24h rate-limit pre-flight counting `status='success'` rows from `ai_extraction_log` (limit 20/org/24h) → service-role upload of the file bytes to `event-extractions/{orgId}/{extractionId}/{ts}.{ext}` → `extractWithClaude()` → `ai_extraction_log` row with the extraction UUID, tokens, and storage path → truncated event returned with `meta = { source_mime, source_bytes, extraction_id, model, tokens_input, tokens_output }`. Every failure path writes a log row: rate_limited (before Claude call), error (SDK failure), validation_failed (schema mismatch), so rate-limit counts and observability stay accurate. User-facing errors stay generic per the MGT-023 → MGT-029 convention; the rate-limit error is the one explicit user-facing message (`"Daily extraction limit reached. Please try again tomorrow."`). `ExtractionMeta` widened with optional `extraction_id`/`model`/`tokens_input`/`tokens_output` fields and made `mock` optional — Phase A audit rows keep rendering via `AuditLogView`'s generic detail pretty-printer. `saveExtractedEventContent()` now builds the audit detail dynamically (omits undefined fields), and on success links the `ai_extraction_log` row to the new event via `UPDATE ai_extraction_log SET event_id=... WHERE id=extraction_id` through the service-role client. `src/lib/types/database.ts` adds the `ai_extraction_log` table type with all three FK `Relationships` so `createAdminClient()` inserts/updates are strictly typed. Phase A UI, preview component, rollback, and audit-action name unchanged. Remaining deferred per KNOWN_ISSUES: 30-day Storage retention cron, consumer-side `/my/upload`, batch extraction, template-from-extraction, paid-tier gating. New DEC-030 covers the tool-use-over-prose choice and the 20/org/24h soft cap rationale.

- **AI-assisted event creation — Phase A UX scaffold with mock extraction (2026-04-17, MGT-069)**: Adds a third `From PDF / image` tab to `/admin/events/new` alongside `From scratch` and `From template`, wiring the full upload → preview → create-event flow end-to-end with a hardcoded mock extraction response. No `@anthropic-ai/sdk`, no env vars, no migrations, no Storage bucket, no rate limiting — all of those land in MGT-070 Phase B. New files: `src/lib/ai/extract.ts` (shared `ExtractedEvent` / `ExtractedDay` / `ExtractedEntry` types + hand-written type guards mirroring the `isMetaChanges` pattern in `AuditLogView.tsx` since zod is not a dependency, `truncateExtractedEvent()` that applies `FIELD_LIMITS.event.*` / `FIELD_LIMITS.entry.*` to every string before it reaches an insert, and the `MOCK_EXTRACTED_EVENT` two-day karting fixture); `src/lib/ai/extract.test.ts` (vitest smoke tests for the type guards — valid mock accepted, missing/invalid `start_time`, non-ISO dates, `end_date < start_date` all rejected — plus truncation at `FIELD_LIMITS` boundaries); `src/app/admin/events/extract/actions.ts` (the `'use server'` `extractEventFromUpload(formData)` server action that `requireEditor()`-gates, re-validates MIME against `{ application/pdf, image/png, image/jpeg }` and byte length against 10 MB on entry, waits 800 ms to expose the loading state, and returns the truncated mock with `{ source_mime, source_bytes, mock: true }` metadata; plus `saveExtractedEventContent(eventId, days, meta)` that wipes the blank days `createEvent()` auto-inserts, re-inserts labelled days + timetable entries day-by-day, cascade-rolls-back the event on any failure mirroring the `createEventFromTemplate()` pattern, and stamps a `writeAuditLog({ eventId }, 'event.created_from_extraction', { source_mime, source_bytes, mock: true, day_count })` row via the shared DEC-025 `AuditScope` contract and DEC-018 `makeActorContext(membership)` provenance); `src/components/admin/ExtractionPreview.tsx` (editable preview rendering metadata + per-day entries with `CharCounter` bars on every capped input, `MAX_EVENT_DAYS` enforcement, `Create event` + `Discard and start from scratch` buttons). Edits: `src/app/admin/events/new/page.tsx` adds the `'extract'` mode to the existing tab bar, mounts a file input (`accept="application/pdf,image/png,image/jpeg"`), performs client-side MIME + 10 MB size + empty-file validation before the server hit, and on confirm chains `createEvent()` → `saveExtractedEventContent()` → `router.push('/admin/events/{id}')`; `src/components/admin/AuditLogView.tsx` registers `event.created_from_extraction` → `Event created from extraction` in both `actionLabels` and `filterOptions`. Every Sentry capture in the new action uses the existing `action: '<function>.<step>'` sub-tag convention (`extractEventFromUpload.invalidMime`, `.oversize`, `.empty`, `.missingFile`; `saveExtractedEventContent.fetchEvent`, `.wipeDays`, `.insertDay`, `.insertEntries`). All user-facing errors follow the generic MGT-023 → MGT-029 pattern. No DB change, no RLS change, no new runtime dependency. Full vitest suite still green (existing 62 + new extract smoke tests); `tsc --noEmit` clean; `next build` succeeds. New DEC-029 covers the phased delivery strategy; `docs/KNOWN_ISSUES.md` gets an MGT-069 entry documenting the Phase A scope and the items explicitly deferred to MGT-070.

- **Remove dev-only audit fixture route (cleanup, 2026-04-17, MGT-066)**: Deletes `src/app/api/dev/audit-fixture/route.ts` (and the now-empty `src/app/api/dev/` parent folder) now that MGT-065 has verified the `OrgAuditLogView` positive-branch `target_email` rendering end-to-end both in the UI and the CSV export. The route was always scoped as temporary per DEC-028 — dev-only, hard-gated on `NODE_ENV === 'development'` + `DEV_ADMIN_EMAIL` session match + active-org match + owner/admin role — and has no consumers in the repo (grep for `audit-fixture` returned only the route itself, historical MGT-064 / MGT-065 docs entries, and Obsidian editor state; no tests, no scripts, no UI buttons, no imports). DEC-028 status line in `docs/DECISIONS.md` updated from `Active — shipped with MGT-064` to `Closed — dev fixture removed via MGT-066 (2026-04-17)` with a short note that verification is complete and no production impact occurred. No changes to `writeAuditLog()`, the `audit_log` table, RLS policies, migrations, server actions, event-audit, or any audit UI (`AuditLogView`, `OrgAuditLogView`, `NotificationLogView` all untouched). `src/app/api/` tree now only contains `auth/` and `webhooks/` — no orphan `dev/` directory left behind. One file deleted, two docs entries edited (this one + DEC-028 status); no KNOWN_ISSUES entry (no defect).

- **CSV positive `target_email` branch end-to-end verification (verification-only, 2026-04-17, MGT-065)**: Exercised the previously-unreachable non-null branch of the `OrgAuditLogView` summary + CSV formatter ([`src/components/admin/OrgAuditLogView.tsx:155-171`](src/components/admin/OrgAuditLogView.tsx)) live via the MGT-064 dev-only fixture route. With `NODE_ENV=development` and `DEV_ADMIN_EMAIL` bound to the active session, two `POST /api/dev/audit-fixture` calls were made against the `MGT-060 Verify Org` org (`0365eddb-40e3-4f44-be71-3caf447dabb3`): `{ action: 'org_member.role_updated', targetEmail: 'mgt065-fixture@mygridtime.dev', role: { from: 'admin', to: 'owner' } }` → `200 { ok: true }`, and `{ action: 'org_member.removed', targetEmail: 'mgt065-fixture@mygridtime.dev', previousRole: 'admin' }` → `200 { ok: true }`. Refreshing `/admin/orgs/settings` and expanding the Audit log panel rendered both rows with the real email string: **Role changed** · `mgt065-fixture@mygridtime.dev: Admin→Owner` (line 269 branch) and **Member removed** · `Removed mgt065-fixture@mygridtime.dev (was Admin)` (line 285 branch). Exporting CSV produced `org-audit-log-2026-04-17.csv` with header `Timestamp,Action,User,Summary,Detail`; the **Summary** column for the two fixture rows carried `mgt065-fixture@mygridtime.dev: Admin -> Owner` (line 161 branch, ASCII `->`) and `Removed mgt065-fixture@mygridtime.dev (was Admin)` (line 167 branch) respectively, and the **Detail** column preserved the raw payload JSON (`target_email`, `target_user_id`, `org_id`, and either `changes.role.{from,to}` or `previous_role`) for each row so forensic joins are still possible downstream. No regression on the `target_email ?? 'a member'` fallback rendering for live audit rows (the existing rows on the same panel still render `a member` as before). No code, schema, RLS, migration, server-action, or event-audit change; the fixture route remains 404 outside `NODE_ENV === 'development'` and shares no prod call-path except the shared `writeAuditLog()` helper. Closes the last deferred follow-up from MGT-063 / MGT-064. One docs entry (this one); no KNOWN_ISSUES entry (no defect).

- **Dev-only org-audit `target_email` verification fixture (dev-tooling, 2026-04-17, MGT-064)**: Closes the unexercised positive-branch follow-up flagged by MGT-063. Under the `users_select_own` RLS policy the `users.email` join inside `updateMemberRole()` / `removeMember()` always returns `null` for non-self member rows, so in production the `target_email` field on `org_member.role_updated` and `org_member.removed` audit rows is always `null` and the `OrgAuditLogView` summary + CSV formatter branches that render the real email string (`src/components/admin/OrgAuditLogView.tsx:160,165,269,285`) are unreachable on live data. Sole-owner guards at `src/app/admin/orgs/actions.ts:402-413,472-483` block the self-target workaround. Added a new dev-only route handler at `src/app/api/dev/audit-fixture/route.ts` that inserts a synthetic `org_member.role_updated` or `org_member.removed` audit row with a caller-supplied non-null `target_email` via the existing `writeAuditLog()` helper (`src/lib/audit.ts:81`) so the XOR-scope `CHECK` (`audit_log_scope_xor`, DEC-025) and RLS (`audit_log_insert_members` under `get_user_org_role() IN ('owner','admin','editor')`) are the same invariants that gate every production write. Mirrors the `/api/auth/dev-session` hard-gate verbatim (`NODE_ENV === 'development'` → returns `404` in all other environments; additionally requires `DEV_ADMIN_EMAIL` to be set and the session email to match case-insensitively). Request body: `{ orgId: string, action: 'org_member.role_updated' | 'org_member.removed', targetEmail: string, previousRole?: 'owner'|'admin'|'member'|'editor'|'viewer', role?: { from, to } }`. The route resolves `activeOrg` via the same `getActiveOrg()` utility used by `requireOwnerOrAdmin()`, requires the `orgId` in the body to match the caller's active org, requires an owner/admin role, and stamps `actor_context` through the shared `makeActorContext()` helper so the fixture row is structurally indistinguishable from a real production write (same `via`, same `platform_role` handling). `target_user_id` is set to the actor's own id — the fixture does not reference any other real member. `detail` payload shape matches `src/app/admin/orgs/actions.ts:426-438` (`role_updated`) and `:495-507` (`removed`) exactly — `{ org_id, target_user_id, target_email, changes: { role: { from, to } } }` and `{ org_id, target_user_id, target_email, previous_role }`. No backend production changes: the route 404s outside dev, shares no code with prod call sites except the shared `writeAuditLog()` helper, and touches no schema, no migrations, no event-audit, no `updateMemberRole()` / `removeMember()` control flow. **Verification**: with `NODE_ENV=development` and `DEV_ADMIN_EMAIL` set, `POST /api/dev/audit-fixture` with `{ orgId, action: 'org_member.role_updated', targetEmail: 'fixture.user@example.dev', role: { from: 'member', to: 'admin' } }` → refreshing `/admin/orgs/settings` renders the row with `fixture.user@example.dev: Member → Admin` in the summary (line 269 branch) and the same string in the CSV Summary column (line 160 branch). Repeating with `action: 'org_member.removed'` and `previousRole: 'admin'` exercises the line 285 / 165 branches. Prod-safety: `NODE_ENV=production` build + same `curl` → `404`. See DEC-028. One new file (the route), one docs entry (this one), one decision entry (MGT-064 resolved).

- **Self-email-present branch verification for `org_member.role_updated` + `org_member.removed` summaries (verification-only, 2026-04-17, MGT-063)**: Closes the unexercised positive-path sibling to MGT-060 / MGT-062. Those two prior tickets fixed and verified the `target_email == null → "a member"` fallback (the common case under `users_select_own` RLS when an owner targets another member). MGT-063 verifies the opposite branch — when `target_email` is a non-null string, the renderer and CSV formatter emit the literal email instead of the fallback. **Verification-by-inspection** of all four call sites in `src/components/admin/OrgAuditLogView.tsx`: UI `role_updated` at line 269 (`const who = target_email ?? 'a member'` → rendered inside `<span className="text-gray-700">{who}</span>`), UI `removed` at line 285 (`<span className="text-gray-700">{target_email ?? 'a member'}</span>`), CSV `role_updated` at line 160 (same ternary → interpolated into the Summary column), CSV `removed` at line 166 (same ternary → interpolated). All four branches are pure null-coalescing over the exact same string field the server actions write (`updateMemberRole` at `src/app/admin/orgs/actions.ts:425-438` emits `target_email: targetEmail`; `removeMember` at `:495-507` emits `target_email: targetEmail` — both paths read the same `(member.users as unknown as { email: string } | null)?.email ?? null` expression at `:400` / `:470`). **No live UI flow exercises the positive path in the current `MGT-060 Verify Org` test state**: the only reachable case where `target_email` is non-null on a `role_updated` / `removed` row is a self-targeted action (because `users_select_own` RLS at the `users!org_members_user_id_fkey(email)` join only returns the email for the actor's own row), and both `updateMemberRole` (sole-owner guard at `:402-413`) and `removeMember` (sole-owner guard at `:472-483`) refuse self-actions when the acting user is the only owner of the org. Reproducing the positive path live would require (a) a second `owner` in the same org so the sole-owner guard releases, then (b) an actor performing `role_updated` or `removed` against themselves — MGT-063 did not seed that state because the branch correctness is already established by inspection: `target_email ?? 'a member'` is a trivially total function over `string | null | undefined`, and the two emission sites pass the exact same `targetEmail` value with no transformation. CSV export path verified to route through the same `formatDetailForCsv()` call (line 179 in `entriesToCsv`), so UI and CSV can never diverge on this field. `npm run typecheck` clean; no code, schema, RLS, migration, or event-audit change. One doc entry (this one), no KNOWN_ISSUES entry (no defect) — MGT-063 resolved.

- **`org_member.role_updated` summary field-mismatch fix + end-to-end verification (UI-only, 2026-04-17, MGT-062)**: Follow-up to the out-of-scope sibling flagged by MGT-060. Verified end-to-end that `OrgAuditLogView`'s `role_updated` branch was dead exactly as predicted — the renderer's `MemberRoleDetail` type declared `email?`, `from?`, `to?`, `old_role?`, `new_role?` and both the UI renderer and CSV formatter destructured `{ email, from, to, old_role, new_role }`, but `updateMemberRole()` at [src/app/admin/orgs/actions.ts:425-438](src/app/admin/orgs/actions.ts) actually writes `{ org_id, target_user_id, target_email, changes: { role: { from, to } } }`. Pre-fix dev-server pass against `MGT-060 Verify Org` (seeded `mgt062-testmember@mygridtime.dev` via service-role at role `admin`, then promoted `admin → owner` + demoted `owner → admin` through the UI): both audit rows rendered with the action label + actor + timestamp and **no summary line**; CSV Summary column was empty for both rows; Detail column preserved full JSON (forensic value intact). **Fix** in `src/components/admin/OrgAuditLogView.tsx` mirrors the MGT-060 pattern verbatim — (a) `MemberRoleDetail` retyped to `{ target_email?: string | null; changes?: { role?: { from?: string; to?: string } } }` (legacy `email`/`from`/`to`/`old_role`/`new_role` keys dropped — the action has always emitted the `target_email` + `changes.role` shape since MGT-057, no historical payloads carry the legacy keys); (b) CSV formatter (lines 155-162) now reads `target_email` + `changes?.role?.{from,to}`, applies `target_email ?? 'a member'`, returns `''` only when both `from` and `to` are missing; (c) UI renderer (lines 264-279) same field reads, `target_email ?? 'a member'` fallback, keeps the red/green from/to colour treatment, early-returns `null` only when both `from` and `to` are missing. Post-fix re-verify: panel renders `a member: Admin → Owner` (promote) and `a member: Owner → Admin` (demote) with the red/green arrow styling matching sibling actions; CSV Summary column carries `a member: Admin -> Owner` and `a member: Owner -> Admin`; no regression on the existing `Removed a member (was Editor)` / `Removed a member (was Admin)` rows or on the branding/org-created summaries. MGT-058 `refreshSignal` wiring still fires on every role change so the new row appears without manual reload. `npm run typecheck` clean; full vitest suite 62/62 green. No backend, schema, RLS, migration, server-action, or event-audit change; one code file edited plus this entry and a KNOWN_ISSUES.md entry (MGT-062 resolved; MGT-060's "flagged for a follow-up" pointer on `role_updated` is now closed).

- **Org audit log summary verification + `org_member.removed` field-mismatch fix (UI-only, 2026-04-17, MGT-060)**: End-to-end verification of the three previously-unexercised summary cases polished in MGT-059 on a fresh org `MGT-060 Verify Org` — two passed as-is, one failed and was fixed in the same pass. **Pass — `organisation.created`**: panel renders `Created "MGT-060 Verify Org"` with action label **Organisation created**; CSV Summary column renders `Created "MGT-060 Verify Org" (slug: mgt-060-verify-org)`. **Pass — `organisation.branding_updated` `logoUrl`**: set / updated / cleared sub-cases each render `Logo URL: set` / `Logo URL: updated` / `Logo URL: cleared` in both the UI and CSV; regression guard confirmed — a combined save with `logoUrl` (null → string) + `primaryColor` (null → `#FF0000`) on the same row renders `Logo URL: set` (prose via `logoUrlSummary()`) and `Primary color: —→"#FF0000"` (from→to via `fmtVal()`) in separate `<li>` children of the same `MetaDiff`, confirming field-aware branching works without cross-contamination. **Fail → fix — `org_member.removed`**: renderer's `MemberRemovedDetail` type declared `email?: string` and both the UI renderer (`MemberActionSummary`) and CSV formatter (`formatDetailForCsv`) destructured `{ email, previous_role }`, but the server-side `removeMember()` action (`src/app/admin/orgs/actions.ts:495-507`) writes `target_email`, not `email`. Both call sites began `if (!email) return …` so they bailed before rendering; every `org_member.removed` row in the panel had no summary line (raw JSON in CSV Detail column preserved forensic value, so no data loss). Compounded by the documented `users_select_own` RLS behaviour at `src/app/admin/orgs/actions.ts:457-458` — `target_email` is nulled for non-self rows, so even after renaming the field the bail would still fire. Fix in `src/components/admin/OrgAuditLogView.tsx` only: `MemberRemovedDetail` type corrected to `{ target_email?: string | null; previous_role?: string }`; renderer now reads `target_email`, falls back to `a member` when null, and only early-returns when **both** `target_email` and `previous_role` are missing so a role-only row still renders `Removed a member (was Admin)`; CSV formatter applies the same fallback so the Summary column carries `Removed a member (was Editor)` / `Removed <email> (was Admin)` consistently with the UI. Re-verified by inserting a non-owner member at role `admin`, removing via the UI → row renders `Removed a member (was Admin)`; re-inserting at role `editor`, removing → `Removed a member (was Editor)`; CSV Summary column matches. MGT-059's sibling `role_updated` path has the same latent `email` vs `target_email` mismatch and the same `from`/`to` vs `changes.role.{from,to}` shape mismatch — **out of scope for MGT-060** (not in the three cases the user scoped) and flagged for a follow-up. `npm run typecheck` clean; full vitest suite 62/62 green. No backend, schema, RLS, migration, server-action, or event-audit change; one code file edited plus this entry and a KNOWN_ISSUES.md entry (MGT-060 resolved).

- **Org audit log action labels and detail polish (UI-only, 2026-04-17, MGT-059)**: `src/components/admin/OrgAuditLogView.tsx` tightens the labels, one-line summaries, and CSV output of the eight org-scoped audit actions surfaced by MGT-057 — no backend, schema, RLS, migration, server-action, or event-audit change. Labels standardised: `organisation.updated` → **Organisation renamed** (accurate — `updateOrganisation` only ever emits this on a name change; slug cannot mutate post-creation), `org_member.invited` → **Invite sent** (aligns with the two sibling invite labels), `org_member.role_updated` → **Role changed**; the other five labels keep their wording. `actionLabels` and `filterOptions` updated in lockstep so the filter dropdown matches the row labels. New `OrganisationCreatedSummary` sub-renderer adds the previously-missing one-liner for `organisation.created` (`Created "{name}"`, reading `detail.name`). `MemberActionSummary` now title-cases all role names via a new local `titleCaseRole()` helper so `admin` / `editor` / `viewer` / `owner` render as **Admin** / **Editor** / **Viewer** / **Owner** wherever a role appears (invited, invite-accepted, role-changed from/to, removed's "was" suffix). `org_member.removed` now surfaces `previous_role` as `Removed {email} (was Admin)` — the field was already in the emitted payload (`src/app/admin/orgs/actions.ts:499`) but previously discarded at render time. `MetaDiff` gains a field-aware branch for `logoUrl`: rather than rendering a full URL pair that wraps ugly at panel width, it shows **set** (null → string), **cleared** (string → null), or **updated** (string → string) via a new `logoUrlSummary()` helper; `primaryColor` and `headerText` keep the existing `"from" → "to"` rendering verbatim. CSV export gains a fifth **Summary** column between **User** and **Detail** — the existing **Detail** column stays as raw `JSON.stringify(detail)` so downstream scripts and spreadsheet joins on specific payload keys lose nothing, and the new Summary column carries the same human-readable one-line prose the UI shows (e.g. `Created "Acme Events" (slug: acme-events)`, `Name: "Old Co" -> "New Co"`, `Primary color: "#111" -> "#222"; Logo URL: updated`, `Invited alice@acme.com as Admin`, `alice@acme.com: Editor -> Admin`, `Removed alice@acme.com (was Admin)`). A new `formatDetailForCsv(action, detail)` helper drives the Summary column and returns `''` for unknown/malformed payloads (the raw JSON in the next column preserves forensic value). Header row: `Timestamp,Action,User,Summary,Detail`. Filename unchanged (`org-audit-log-YYYY-MM-DD.csv`). Search input unchanged — the memoised `filteredEntries` pipeline still stringifies the raw `detail` payload, so searching by role (e.g. `admin`) still matches role-updated rows even though the visible label now title-cases them. `AuditLogView.tsx` untouched per scope; event audit unchanged. One source file plus this status entry (MGT-059 resolved).

- **Org audit log live-refresh wiring (UI-only, 2026-04-17, MGT-058)**: `OrgAuditLogView`'s previously-inert `refreshSignal` prop is now driven by the three interactive forms on `/admin/orgs/settings`, so newly written org audit rows appear in the panel without a page reload. New client wrapper `src/app/admin/orgs/settings/SettingsPanels.tsx` (`'use client'`) owns `const [refreshSignal, setRefreshSignal] = useState(0)` and a `bumpRefresh = () => setRefreshSignal((n) => n + 1)` helper, renders the four interactive sections (organisation name, slug, branding, members & invites, audit log) plus the read-only slug card inside the existing `<section>` + `H2` markup, and threads `onSaved={bumpRefresh}` into `OrgNameForm`, `BrandingForm`, and `MemberManager` while threading `refreshSignal={refreshSignal}` into `OrgAuditLogView`. `src/app/admin/orgs/settings/page.tsx` stays a server component — keeps auth + `getActiveOrg` + org fetch + the existing `Promise.all` that parallel-fetches `listOrgMembers` / `listOrgInvites` / `loadAuditLog({ orgId })` — and delegates all section rendering to `<SettingsPanels … />`, keeping only the breadcrumb, H1, and subtitle at the server layer. Forms gain a new optional `onSaved?: () => void` callback: `OrgNameForm` invokes it after `setSuccess(true)` and before `router.refresh()` (so the server `org.name` still re-reads for the header while the audit panel refetches independently); `BrandingForm` invokes it after `setSuccess(true)`; `MemberManager` invokes it at all four success sites (`confirmRoleChange`, `confirmRemove`, `handleInvite`, `handleRevokeInvite`) after `loadData()` resolves, regardless of whether the local member-list refresh succeeded — the audit-log refresh is independent of the members-list refresh so a transient `listOrgMembers` / `listOrgInvites` failure still lets the audit row surface. `Member` and `Invite` types in `MemberManager.tsx` are now `export`ed so `SettingsPanels` can type its props. Mirrors the `EventEditor.auditRefreshSignal` → `AuditLogView.refreshSignal` pattern — `OrgAuditLogView`'s existing `useEffect` that flips `allLoaded = false` when `refreshSignal > 0` (live since MGT-057) now fires on every successful org-settings save, triggering its internal `loadAuditLog({ orgId })` refetch. No backend, schema, RLS, migration, server-action, or event-audit change; `AuditLogView` and `EventEditor` remain untouched per the task constraint. The "Currently inert — org settings forms don't bump it yet." comment on `OrgAuditLogView`'s `refreshSignal` prop is updated to reflect the wired state (MGT-058 resolved).

- **Org audit log UI (UI-only, 2026-04-17)**: New `OrgAuditLogView` client component at `src/components/admin/OrgAuditLogView.tsx` surfaces org-scoped `audit_log` rows on `/admin/orgs/settings`. Mounted as a new **Audit log** section at the bottom of `src/app/admin/orgs/settings/page.tsx`, fed an initial page of entries fetched server-side via `loadAuditLog({ orgId })` in parallel with the existing members/invites fetch. Near-copy of `AuditLogView` (event-scoped UI is untouched per the MGT-057 constraint) with org-specific `actionLabels` and `filterOptions` covering the eight org actions emitted by `src/app/admin/orgs/actions.ts` (`organisation.created` / `organisation.updated` / `organisation.branding_updated` / `org_member.invited` / `org_member.invite_revoked` / `org_member.invite_accepted` / `org_member.role_updated` / `org_member.removed`), `metaFieldLabels` for `name` / `slug` / `primaryColor` / `logoUrl` / `headerText`, `MetaDiff` reused verbatim for `organisation.updated` + `organisation.branding_updated` `{ changes }` payloads, and a new inline `MemberActionSummary` renderer that produces one-line human summaries for member actions (e.g. "Invited alice@example.com as admin", "Changed bob@example.com from admin to editor") with graceful fallback when fields are missing. Panel is collapsible, auto-loads on open, supports the same action-filter dropdown + text search + date-range filter + CSV export (filename `org-audit-log-YYYY-MM-DD.csv`), carries the 2000-row safety cap banner, and honours the same MGT-030 retry semantics on failure. `refreshSignal` prop is accepted and defaults to `0` — org settings forms don't bump it yet; wired for later. Page-level owner/admin gate at `src/app/admin/orgs/settings/page.tsx:29` and the RLS policy `audit_log_select_admin` (widened in MGT-055) jointly enforce access — non-owners/admins are redirected before the panel renders, and RLS silently hides any rows they shouldn't see. No backend, schema, RLS, or migration change; no change to `AuditLogView`, the event audit panel on `/admin/events/{id}`, or any `writeAuditLog()` call site. Closes the last deferred follow-up from MGT-055 (MGT-057 resolved).

- **NotificationLogView filter-bar redesign — chip wall → Date range dropdown (UI-only, 2026-04-17)**: `src/components/admin/NotificationLogView.tsx` collapses the crowded second filter row (4 status/type chips + 9 date chips, 13 equal-weight buttons) into a single **Date range** `<select>` placed on the existing Status/Type/Search row (between Type and Search). Dropdown options: **All time**, **Today**, **Yesterday**, **Last 7 days**, **Last 30 days**, **This month**, **Last month**, **This year**, **Last year**. Selected value is derived purely from the existing `is{Today,Yesterday,Last7Days,Last30Days,ThisMonth,LastMonth,ThisYear,LastYear,AllTime}Active` booleans — no new state, no new prop. `handleDateRangeChange()` switch-cases on the key and writes the existing DEC-024 local-calendar ISO pair via `setDateFrom` / `setDateTo`. A disabled **Custom** option surfaces automatically when the current `dateFrom` / `dateTo` doesn't match any preset (e.g. manual From/To input on Row 3), so the dropdown stays honest about the active date state. Removed: 4 redundant status/type chips (Failed, Sent, Published, Timetable updated) — the Status + Type dropdowns on the same row already covered that filter state, the chips duplicated them. Kept verbatim: Row 1's 3 triage preset chips (Failures today / Sent today / Published today), Row 3's From/To date inputs (for custom ranges outside the 9 presets, per explicit user choice), Clear filters, and Export CSV. Reuses `filteredEntries` `useMemo`, `applyPreset()`, `clearAllFilters()`, `chipClass()`, all state hooks, every ISO-derivation line, the Retry banner, cap warning, `refreshSignal` reload, and row renderer without change. No server-action, schema, RLS, migration, or notification-writer change. DEC-024 scope note updated so the local-calendar-ISO rule now applies to the dropdown's switch-case outputs (plus the `Custom` affordance note); no new DEC needed. `npm run typecheck` clean; full vitest suite (62/62) green; `next build` succeeds. Client-side only; one code file touched (MGT-052 resolved).

- **NotificationLogView "All time" quick filter (UI-only, 2026-04-17)**: `NotificationLogView` (`src/components/admin/NotificationLogView.tsx`) gains a ninth chip in the date-preset row — **All time** — rendered immediately after **Last year** at the end of the date-chip row. One-click unconditionally clears both `dateFrom` and `dateTo` to the empty string, returning the panel to full unbounded history. Unlike the eight sibling date chips (each of which writes a `YYYY-MM-DD` pair and toggles off on a second click), **All time** represents the explicit "no date filter" state — its `aria-pressed` derives from `!dateFrom && !dateTo`, so clicking any other date chip unpresses it, and pressing **Clear filters** or clicking an active date chip's toggle-off branch re-presses it automatically. Mutually exclusive with the eight date-value chips by construction. Critically, **All time** only mutates date-range state — it never touches `statusFilter`, `typeFilter`, or `searchQuery`, so an admin filtering for `status = failed` can pivot between a bounded window and full history without losing their status/type/search selection; **Clear filters** remains the only affordance that resets all five filter state pieces at once. Reuses the existing `chipClass()` helper, `controlsDisabled` gate, `aria-pressed` pattern, and the same neutral gray active styling as its date-chip siblings (`bg-gray-100 text-gray-800 border-gray-300`). No new state, no new filter logic, no new prop, no server-action/schema/RLS/notification-writer change. DEC-024 updated — **All time** is explicitly outside its scope because it writes no date string (the rule governs chips that *produce* `YYYY-MM-DD` values). Client-side only; one code file touched.

- **NotificationLogView "Last year" quick filter (UI-only, 2026-04-17)**: `NotificationLogView` (`src/components/admin/NotificationLogView.tsx`) gains an eighth date-preset chip — **Last year** — rendered immediately after **This year** at the end of the date-preset row. One-click sets `dateFrom` to the 1st of January of the previous calendar year and `dateTo` to the 31st of December of the previous calendar year (both via `toLocalIsoDate(new Date(y - 1, 0, 1))` and `toLocalIsoDate(new Date(y - 1, 11, 31))`); clicking again clears both date inputs. Unlike **This year** (year-to-date, Jan 1 → today), **Last year** covers the full previous calendar year end-to-end for annual reviews and compliance spot checks. Reuses the existing `toLocalIsoDate()` helper (DEC-024), `chipClass()` helper, `dateFrom` / `dateTo` state, `controlsDisabled` gate, `aria-pressed` active state, and the existing `filteredEntries` `useMemo` date-range clauses — no new state, no new filter logic, no new prop, no server-action/schema/RLS/notification-writer change. Active state is derived via a new `isLastYearActive` boolean alongside the other preset booleans. Chip uses the same neutral gray active styling as its date-preset siblings (`bg-gray-100 text-gray-800 border-gray-300`). Client-side only; one file touched. DEC-024 unchanged (behaviour fully covered).

- **NotificationLogView "This year" quick filter (UI-only, 2026-04-17)**: `NotificationLogView` (`src/components/admin/NotificationLogView.tsx`) gains a seventh date-preset chip — **This year** — rendered immediately after **Last month** in the date-preset row. One-click sets `dateFrom` to the 1st of January of the current calendar year (via `toLocalIsoDate(new Date(y, 0, 1))`) and `dateTo` to today; clicking again clears both date inputs. Reuses the existing `toLocalIsoDate()` helper (DEC-024), `chipClass()` helper, `dateFrom` / `dateTo` state, `controlsDisabled` gate, `aria-pressed` active state, and the existing `filteredEntries` `useMemo` date-range clauses — no new state, no new filter logic, no new prop, no server-action/schema/RLS/notification-writer change. Active state is derived via a new `isThisYearActive` boolean alongside the other preset booleans. Chip uses the same neutral gray active styling as its date-preset siblings (`bg-gray-100 text-gray-800 border-gray-300`). Client-side only; one file touched. DEC-024 unchanged (behaviour fully covered).

- **NotificationLogView "Last month" quick filter (UI-only, 2026-04-17)**: `NotificationLogView` (`src/components/admin/NotificationLogView.tsx`) gains a sixth date-preset chip — **Last month** — rendered immediately after **This month** in the date-preset row. One-click sets `dateFrom` to the 1st of the previous calendar month (via `new Date(y, m - 1, 1)` — constructor handles Jan → Dec-of-prior-year automatically) and `dateTo` to the last day of that month (via `new Date(y, m, 0)` — day 0 of the current month, which the `Date` constructor resolves to the last day of the previous month including leap-year February); clicking again clears both date inputs. Unlike **Last 30 days** (a rolling window) and **This month** (1st-of-month-to-today), **Last month** covers the full previous calendar month end-to-end, answering the common "how did delivery go last month?" triage question in one click. Reuses the existing `toLocalIsoDate()` helper (DEC-024), `chipClass()` helper, `dateFrom` / `dateTo` state, `controlsDisabled` gate, `aria-pressed` active state, the existing `filteredEntries` `useMemo` date-range clauses, and the existing `hasActiveFilters` composition — no new state, no new filter logic, no new prop, no server-action/schema/RLS/notification-writer change. Active state is derived via a new `isLastMonthActive` boolean alongside the other preset booleans. Chip uses the same neutral gray active styling as its date-preset siblings (`bg-gray-100 text-gray-800 border-gray-300`). Client-side only; one file touched. DEC-024 updated to list six chips rather than five.

- **NotificationLogView date-chip helpers standardised on local-calendar model (UI-only, 2026-04-17)**: `src/components/admin/NotificationLogView.tsx` previously mixed UTC-derived `new Date().toISOString().slice(0, 10)` strings (for `todayIso` / `yesterdayIso` / `sevenDaysAgoIso` / `thirtyDaysAgoIso`, using DST-unsafe millisecond subtraction) with a local-midnight-through-UTC-ISO `firstOfMonthIso`, while the same strings were parsed as local in the filter pipeline (`new Date(dateFrom + 'T00:00:00')`) and consumed by local-valued `<input type="date">` controls. The mismatch produced an off-by-one **Today** between 00:00–01:00 local in any `UTC+N` zone and a wrong **This month** that spanned into the previous month on the 1st in any positive offset — no correctness issue during UK winter, but broken every BST summer after midnight and on the 1st of any month. Fix: a single file-local helper `toLocalIsoDate(d: Date): string` (positioned next to `formatTimestamp` / CSV helpers) formats a `Date` via local `getFullYear` / `getMonth` / `getDate` components with zero-padding, and all five chip strings now derive from it with offsets applied through the `Date` constructor (`new Date(y, m, d - 1 | 6 | 29)`), which handles day/month/year rollover and DST transitions correctly. Consumers are unchanged — the five `is{Today,Yesterday,Last7Days,Last30Days,ThisMonth}Active` comparisons, the `applyPreset()` helper, the three daily-triage presets (Failures today / Sent today / Published today), and every chip `onClick` all inherit the corrected values automatically. `AuditLogView.tsx` left alone by design — its one `toISOString().slice(0, 10)` use is a CSV filename label, not a filter input. Client-side only; one file touched; no server action, schema, RLS, migration, or notification-writer change. `npm run typecheck` clean; full vitest suite (62/62) green. Dev-server manual pass on `/admin/events/{id}` in Europe/London (BST): **Today** = 2026-04-17/2026-04-17, **Yesterday** = 2026-04-16/2026-04-16, **Last 7 days** = 2026-04-11/2026-04-17, **Last 30 days** = 2026-03-19/2026-04-17, **This month** = 2026-04-01/2026-04-17, all three triage presets drive to today and toggle `aria-pressed` correctly, every chip clears both date inputs on a second click; no console errors (MGT-051 resolved; codified as DEC-024).

- **NotificationLogView "This month" quick filter (UI-only, 2026-04-17)**: `NotificationLogView` (`src/components/admin/NotificationLogView.tsx`) gains a fifth date-preset chip — **This month** — rendered immediately after **Last 30 days** in the date-preset row. One-click sets `dateFrom` to the 1st of the current calendar month (local time, via `new Date(now.getFullYear(), now.getMonth(), 1)` → ISO → slice(0,10), consistent with the existing `todayIso` approach) and `dateTo` to today; clicking again clears both date inputs. Unlike **Last 30 days** (a rolling window), **This month** aligns with calendar-month boundaries, answering the common "how did delivery go this month?" triage question in one click. Reuses the existing `chipClass()` helper, `dateFrom` / `dateTo` state, `controlsDisabled` gate, `aria-pressed` active state, the existing `filteredEntries` `useMemo` date-range clauses, and the existing `hasActiveFilters` composition — no new state, no new filter logic, no new prop, no server-action/schema/RLS/notification-writer change. Active state is derived via a new `isThisMonthActive` boolean alongside the other preset booleans. Chip uses the same neutral gray active styling as its date-preset siblings (`bg-gray-100 text-gray-800 border-gray-300`). Client-side only; one file touched. `npm run typecheck` clean; full vitest suite (62/62) passes.

- **NotificationLogView "Last 7 days" quick filter (UI-only, 2026-04-17)**: `NotificationLogView` (`src/components/admin/NotificationLogView.tsx`) gains a third date-preset chip — **Last 7 days** — rendered immediately after the existing **Today** / **Yesterday** chips in the date-preset row. One-click sets `dateFrom` to 6 calendar days before today and `dateTo` to today (inclusive window spans 7 calendar days); clicking again clears both date inputs. Reuses the existing `chipClass()` helper, `dateFrom` / `dateTo` state, `controlsDisabled` gate, and the existing date-range filter clauses inside the `filteredEntries` `useMemo` — no new state, no new filter logic. Active state is derived via a new `isLast7DaysActive` boolean alongside `isTodayActive` / `isYesterdayActive`, and the chip uses the same neutral gray active styling as its siblings (`bg-gray-100 text-gray-800 border-gray-300`). `hasActiveFilters` already includes `dateFrom || dateTo`, so the "Clear filters" button lights up automatically when the preset is engaged — no change needed there. No server-action, schema, RLS, or notification-writer changes. Client-side only; one file touched.

- **NotificationLogView CSV export (UI-only, 2026-04-17)**: `NotificationLogView` (`src/components/admin/NotificationLogView.tsx`) gains an **Export CSV** button on row 2 of the existing filter bar (date-range row), right-aligned via `ml-auto`, matching the `AuditLogView` export pattern verbatim. Three module-local helpers (`csvEscape`, `entriesToCsv`, `downloadCsv`) mirror `AuditLogView.tsx:134-162` — `csvEscape` and `downloadCsv` are verbatim copies; `entriesToCsv` is adapted to notification columns (`Timestamp`, `Type`, `Recipient`, `Status`, `Sent at`, `Error`) and reuses the existing `typeLabels` map so the exported Type column matches the visible row label (`Published` / `Timetable updated`). `handleExportCsv()` exports `filteredEntries` only — not the full `allEntries` set — so the export always reflects the current Status / Type / date-range selection (matches `AuditLogView.tsx:343-347`). Button is `disabled={controlsDisabled || filteredEntries.length === 0}` so it blocks during initial load and when filters yield zero rows. Filename pattern: `notification-log-YYYY-MM-DD.csv`. No changes to props, state, filter pipeline, server actions, schema, RLS, or `loadAllNotificationLog`. Client-side only — no backend, no shared-utility extraction (per-component CSV helpers match the existing `AuditLogView` convention). Manual browser verification on dev server: button rendered right-aligned; disabled when status filter = `failed` yielded zero rows; CSV captured via a `Blob` shim confirmed correct header, row count matching filter selection, and proper type-label mapping. Full Vitest suite (62 tests) passes, `tsc --noEmit` clean.

- **NotificationLogView date-range filter (UI-only, 2026-04-17)**: `NotificationLogView` (`src/components/admin/NotificationLogView.tsx`) gains a second filter row with **From** and **To** `<input type="date">` fields, matching the `AuditLogView` date-range pattern verbatim — inclusive day-level filtering (`from = new Date(dateFrom + 'T00:00:00')`, `to = new Date(dateTo + 'T23:59:59.999')`) applied against `entry.created_at` inside the existing `useMemo` filter pipeline. The previous single-row filter bar (Status + Type) is wrapped into a `space-y-2` container so both rows share the same bordered filter-bar shell; row 1 keeps Status + Type, row 2 holds the date inputs. Date inputs are `disabled={controlsDisabled}` while the initial load is in flight, carry `aria-label="Filter from date"` / `"Filter to date"`, and use the same `text-xs border border-gray-200 rounded …` classes as `AuditLogView` for visual parity. Filter-aware empty state now includes `dateFrom || dateTo` in the check, so a zero-match date window renders `"No entries match the current filters."`. Header unfiltered count unchanged. No server-action, schema, RLS, or notification-writer changes; no search, no CSV export. Client-side only — no backend, no new design tokens. Per-task scope.

- **NotificationLogView status + type filters (UI-only, 2026-04-17)**: `NotificationLogView` (`src/components/admin/NotificationLogView.tsx`) gains two client-side filter dropdowns in the panel filter bar — **Status** (`all` / `sent` / `failed` / `queued`) and **Type** (`all` / `event.published` / `timetable.updated`) — reusing the `AuditLogView` filter pattern (filter bar rendered only when `allEntries.length > 0`, `disabled={controlsDisabled}` while the initial load is in flight, AND-composition via a `useMemo` pipeline, filter-aware empty state `"No entries match the current filters."` vs `"No notifications sent yet."`). The collapsible header count continues to show the unfiltered total — consistent with `AuditLogView`. No search, date-range, or CSV export in this pass (intentionally minimal per task scope). No server-action change, no schema change, no RLS change, no notification-writer change. Full Vitest suite (62 tests) passes, `tsc --noEmit` clean.

- **Notification history panel on admin event page (UI + read-only action, 2026-04-17)**: `notification_log` rows were being written correctly by `sendEventNotification()` in `src/lib/resend/notifications.ts` on every publish / timetable-save notification attempt, but had no read surface in the admin UI — admins wanting to confirm delivery or diagnose a failed send had to inspect Supabase directly. A new read-only **Notification history** panel now renders below the existing **Audit log** panel on `/admin/events/{id}`, mirroring `AuditLogView`'s exact shell (collapsible header, initial server-side fetch of 25 rows + `hasMore`, Sentry-tagged error capture, inline Retry banner via the MGT-030 / MGT-037 rescue pattern, parent-driven refresh on save) so admins get a consistent affordance and no new UI patterns are introduced. New server action `loadAllNotificationLog(eventId)` in `src/app/admin/events/actions.ts` (and exported `NotificationLogEntry` type) follows the `loadAllAuditLog()` pattern verbatim: `requireEditor()` gate, 2000-row safety cap, Sentry sub-tag `loadAllNotificationLog.select`, generic user-facing error `"Could not load notification history. Please retry."`. `src/app/admin/events/[id]/page.tsx` fetches the initial page with Sentry tag `eventEditorPage.listNotifications` and threads `notificationLog`, `notificationHasMore`, `notificationLoadError` props into `EventEditor`. New client component `src/components/admin/NotificationLogView.tsx` mirrors `AuditLogView`'s shell minus the filter bar / search / date-range / CSV export (per task constraint "no filters or pagination beyond basic reuse patterns") — it renders one row per entry with recipient email, type label (`event.published` / `timetable.updated`), a colour-coded status pill (`sent` green / `failed` red / `queued` amber), and timestamp; failed rows surface the `error` string inline in muted red. `EventEditor` gains a `notificationRefreshSignal` counter incremented inside `performTimetableSave()` immediately adjacent to the existing `auditRefreshSignal` bump — scoped to the timetable-save path only, since DEC-002 means metadata save cannot trigger a notification_log write. Desktop section-anchor nav at the top of the editor adds a **Notifications** jump link alongside the existing Audit link. No changes to the notification writing system (`sendEventNotification` untouched), no schema change, no migration, no RLS change, no new design tokens (MGT-047 resolved).

- **Audit log auto-refresh after timetable save (UI-only, 2026-04-17)**: `AuditLogView` (`src/components/admin/AuditLogView.tsx`) gains a new optional `refreshSignal?: number` prop. A guarded `useEffect` flips `allLoaded` back to `false` when the signal changes (and is `> 0`, so mount does not double-fire), reusing the existing panel-open `loadAll()` retry path (same one the inline Retry banner triggers). `EventEditor` (`src/components/admin/EventEditor.tsx`) adds an `auditRefreshSignal` counter (`useState(0)`) and increments it inside `performTimetableSave()` on the successful path, immediately before the existing `router.refresh()` call, and threads it through to `<AuditLogView … refreshSignal={auditRefreshSignal} />`. Closes the gap where a saved timetable change would not appear in the already-loaded audit log panel until the user collapsed/re-opened it or reloaded the page (client-side `allLoaded` cache short-circuited the on-open effect, so `router.refresh()` alone could not re-populate the list). `performMetaSave()` is deliberately not wired in this pass — scope limited to `saveDayEntries` per task boundary. No server-action, `saveDayEntries`, `VersionHistory`, or notification-system changes. Full vitest suite (62 tests) passes, `tsc --noEmit` clean, `next build` succeeds (DEC-023).

- **Dev-session route — complete fix (2026-04-17)**: `/api/auth/dev-session` now returns 302 → `/admin` with real Supabase session cookies in local dev, authenticating `DEV_ADMIN_EMAIL`. Root cause of the prior failure: `@supabase/ssr`'s `createServerClient` hard-codes `auth.flowType: 'pkce'` (applied after caller's options spread at `node_modules/@supabase/ssr/src/createServerClient.ts:190`, so un-overridable), and admin-minted magic-link hashes from `admin.generateLink({ type: 'magiclink' })` are written in the implicit flow — a PKCE client cannot redeem them, so `verifyOtp` returned `"Email link is invalid or has expired"` regardless of whether the verify `type` was `'email'` or `'magiclink'`. Fix: `src/app/api/auth/dev-session/route.ts` now skips `generateLink`/`verifyOtp` entirely. It sets a one-shot random password on the admin user via the service-role client (`admin.auth.admin.updateUserById(id, { password })` with `dev-${crypto.randomUUID()}`) and then calls `supabase.auth.signInWithPassword()` through a `createServerClient` cookie adapter — password sign-in is PKCE-compatible, so the adapter collects real `sb-*` session cookies which are attached to the `NextResponse.redirect('/admin')` response. The password is never persisted as a stable credential and is regenerated per request. No other files touched. The route remains hard-gated on `NODE_ENV === 'development'` (404 in all other environments). Full vitest suite (62 tests) passes, `tsc --noEmit` clean, `next build` succeeds. Manual verification on dev server: `GET /api/auth/dev-session` returned 307 with `sb-*-auth-token` cookie set; follow-up `GET /admin` returned 200 with authenticated dashboard (`<h1>Events</h1>`). DEC-010 implementation note updated with the PKCE/implicit-flow diagnosis; MGT-046 fully resolved.

- **Timetable review modal — explicit notify actions (UI-only, 2026-04-17)**: The single "Notify attendees about changes" checkbox in the `ReviewModal` footer (`src/components/ui/ReviewModal.tsx`) is removed and replaced with two explicit primary submit actions — **Save only** (outline secondary, `notify = false`) and **Save and notify** (primary solid, `notify = true`). The two-button variant renders only when `notifyChoiceApplicable === true`; `EventEditor` (`src/components/admin/EventEditor.tsx`) passes it as `reviewMode === 'timetable' && status === 'published' && !!notificationEmails.trim()`. In all other cases (metadata review, unpublished event, or published event with no recipients) the footer continues to render today's single "Save" / "Accept all & save" button unchanged. The `footerExtra` slot still surfaces the "No notification email addresses set for this event." info paragraph when an admin is on the timetable review for a published event without recipients. Callback signatures widened — `onAcceptAll(notify: boolean)`, `onConfirmSave(notify: boolean)`, `onAcceptAndSave(id: string, notify: boolean)` — and `performTimetableSave(rejAddedLocalIds, rejEditedIds, notify: boolean)` threads the chosen flag through to the existing `saveDayEntries(event.id, allEntries, deletedEntryIds, notify)` call. The in-card last-card "Accept & save" and "Undo skip & save" shortcuts keep their existing layout but now explicitly pass `notify = false` — the explicit notify decision is scoped to the footer per the task boundary. `notifyOnSave` state deleted from `EventEditor`. No server-action, `publishEvent`, `sendEventNotification`, or `notification_log` changes. Metadata review unchanged. Publish dialog UI unchanged. Full vitest suite (62 tests) passes, `tsc --noEmit` clean, `next build` succeeds (MGT-045 resolved).

- **Public URL redesign — Pass C1 (org public URLs move to `/{orgSlug}`, 2026-04-16)**: The public organisation page moves from `/o/{orgSlug}` to `/{orgSlug}`. Per-event public URLs remain unchanged at `/{eventSlug}`; both shapes now share the top-level route namespace. Implementation: a new shared constants module `src/lib/constants/reserved-slugs.ts` exports `RESERVED_SLUGS` (the fixed list of top-level static + framework segments — `admin`, `api`, `auth`, `my`, `invites`, `notifications`, `privacy`, `terms`, `o`, `_next`, `favicon.ico`, `robots.txt`, `sitemap.xml`, `manifest.json`, `manifest.webmanifest`, `sitemap`, `robots`, `public`, `static`, `app`, `assets`) and an `isReservedSlug()` helper (case-insensitive, trims input). `createOrganisation()` in `src/app/admin/orgs/actions.ts` now performs three slug-collision checks before insert: (1) `isReservedSlug(slug)` returns `"That slug is reserved. Please choose a different one."`, (2) the existing `organisations.slug` uniqueness probe (renamed `existingOrg`) returns `"That slug is already taken."`, (3) a new `events.slug` uniqueness probe (`existingEvent`) — soft-deleted events are intentionally included since their slug rows remain in the table — also returns `"That slug is already taken."`. The org page rendering JSX is extracted from the legacy route into a new presentational server component `src/components/public/PublicOrgView.tsx` (props: `org`, `events`, `user`) with exported `PublicOrg` and `PublicOrgEvent` types. The org-resolver helper is extracted to `src/lib/utils/public-org.ts` (`resolvePublicOrgBySlug()` — admin-client read with Sentry capture under `tags: { action: 'publicOrgPage.resolveOrg' }`). `src/app/(public)/[slug]/page.tsx` is updated with a fall-through resolution: try published-event lookup first → render event timetable (existing behaviour); otherwise call `resolvePublicOrgBySlug()` → render `<PublicOrgView>` with the org's published, non-deleted events; otherwise `notFound()`. `generateMetadata()` mirrors the same fall-through. The org-events query inside the page captures failures to Sentry with `tags: { action: 'publicOrgPage.listEvents' }` and degrades to an empty event list, matching the previous behaviour. `src/app/(public)/o/[slug]/page.tsx` is replaced with a server component that calls `permanentRedirect('/${params.slug}')` so previously shared `/o/{slug}` URLs (printed collateral, bookmarks, copied addresses) continue to resolve to the new canonical location with a 308. The legacy `o` segment remains in `RESERVED_SLUGS` so a new organisation cannot shadow this redirect. `src/app/sitemap.ts` now emits org URLs as `${baseUrl}/${org.slug}` (instead of `${baseUrl}/o/${org.slug}`); the legacy redirect path is intentionally omitted from the sitemap. `src/app/admin/orgs/settings/PublicOrgUrlField.tsx` doc-comment updated to `/{slug}` (URL string is built by the page; this component is presentational). `src/app/admin/orgs/settings/page.tsx` builds the displayed URL as `${getServerAppUrl()}/${org.slug}` and the slug card help text now reads "shares the top-level URL space with individual event slugs". No DB migration, no RLS change, no event public URL change, no nested-event URL change, no middleware change, no `events.slug` uniqueness check at event-creation time (Pass C2 scope), no UI for changing an existing org slug. Full vitest suite (62 tests) passes, `tsc --noEmit` clean, `next build` succeeds. Supersedes DEC-019; codified as DEC-022. (MGT-044 resolved.)

- **Character-limit pass (UI-only, 2026-04-16)**: Every text input and textarea in the admin event and org editing surfaces now has a defined maximum length, browser enforcement via `maxLength`, and a visible `used/max` character counter. Limits live in a new shared constants file `src/lib/constants/field-limits.ts` (`FIELD_LIMITS.event.*` — title 120, venue 120, notes 1000, notificationEmails 1000, dayLabel 60, templateName 120; `FIELD_LIMITS.entry.*` — title 120, category 60, notes 200; `FIELD_LIMITS.org.*` — name 120, slug 64, primaryColor 7, logoUrl 500, headerText 80, inviteEmail 254). A new tiny presentational component `src/components/ui/CharCounter.tsx` renders the counter with a subtle three-state colour: gray baseline, amber at ≥90%, red at the cap; includes `aria-live="polite"` and `tabular-nums` for stable width. Call sites composed inline across `src/app/admin/events/new/page.tsx` (title/venue/notes), `src/components/admin/EventEditor.tsx` (meta title/venue/notes via an extended `MetaFieldLabel`, notification_emails, duplicate-dialog new title, save-as-template name), `src/components/admin/TimetableBuilder.tsx` (add-day label, inline day rename via a wrapped span), `src/components/admin/EntryRow.tsx` (repeating grid rows: title/category/notes — `FieldWrapper` extended to accept `used`/`max` and render the counter self-end beneath each input, revert-button alignment preserved via `items-start` + `mt-2`), `src/app/admin/orgs/new/page.tsx` (name/slug), `src/app/admin/orgs/settings/OrgNameForm.tsx` (name), `src/components/admin/BrandingForm.tsx` (primaryColor/logoUrl/headerText), and `src/components/admin/MemberManager.tsx` (invite email). Existing field validation/format rules (hex regex on `primaryColor`, email type on invite, required/date validation on event metadata) are preserved verbatim. No form redesign, no new route, no DB migration, no DB/server-side length constraint (enforced at the input layer only in this pass — see DEC-021). Full vitest suite (62 tests) passes, `tsc --noEmit` clean, `next build` succeeds.

- **Admin header Events link (UI-only, 2026-04-16)**: `src/app/admin/layout.tsx` now renders an `Events` nav link (→ `/admin`) in the header's right-hand cluster, positioned immediately to the left of `Settings`. Gated by the same `authorized` condition used for `+ New org`, so the link is visible to the same role set that can actually reach the admin dashboard (`owner` / `admin` / `editor`). Reuses the existing `HEADER_NAV_LINK` styling from `src/lib/styles.ts` — no new style token. No new components, no route changes, no auth/role logic changes, no mobile-layout changes (inherits the existing `flex items-center gap-4` wrap behaviour). Purely additive JSX; full Vitest suite (62 tests) passes, `tsc --noEmit` clean, `next build` succeeds.

- **Role-simplification pass (UI-only, 2026-04-16)**: `MemberManager` (`src/components/admin/MemberManager.tsx`) now exposes only `owner` and `admin` as selectable member roles. Legacy `editor` / `viewer` member rows continue to render their current role via a disabled `<option value={role} disabled>{role} (legacy)</option>` so pre-existing memberships remain visible and unaltered, but those legacy roles cannot be newly assigned from the UI. The invite form no longer contains a role selector — new invites are sent with `role: 'admin'` at the `inviteMember()` call site and the role field renders as a static `admin` label. No changes to server actions (`updateMemberRole()`, `inviteMember()`, `acceptInvite()` still accept all four role strings), no DB migration, no RLS change, no `requireOwnerOrAdmin()` change. Templates test fixture (`src/lib/resend/templates.test.ts`) updated: invite-email `role` fixture changed from `'editor'` to `'admin'` to reflect the new UI-driven invite role and the corresponding assertion string. Full suite (62 tests) passes, `tsc --noEmit` clean, `next build` succeeds (MGT-043 resolved).

## Not Started

- **Consumer dashboard — alerts, drivers, upload** (`/my/alerts`, `/my/drivers`, `/my/upload`): stub pages only
- **Web push notifications**: VAPID keys unused, no service worker, no `web-push` dependency
- **AI timetable extraction**: Phases A + B shipped (MGT-069 → MGT-070). When `MGT_AI_EXTRACTION_ENABLED=true` and `ANTHROPIC_API_KEY` is set, `/admin/events/new` → "From PDF / image" runs a real Claude Vision tool-use call, archives the original upload in the private `event-extractions` Storage bucket, logs every attempt to `ai_extraction_log` (success / error / rate_limited / validation_failed), and enforces 20 extractions per org per rolling 24 hours. Audit rows now carry `{ model, tokens_input, tokens_output, extraction_id, source_mime, source_bytes, day_count }`; legacy Phase A rows continue to render with `{ mock: true, ... }`. Deferred: 30-day Storage retention cron, consumer-side `/my/upload`, batch / multi-file extraction, template creation from extraction, paid-tier gating.
- **Stripe payments**: webhook returns 501, no subscription logic, no `stripe` dependency
- **SMS/WhatsApp notifications**: Twilio not integrated, no `twilio` dependency

## Current Critical Work

All P0, Must Have, and Should Have items (#8–#11) complete. Launch hardening Tier 1 and Tier 2 complete. Next phase is Nice to Have items (#12–#16).

Current behaviour:
- `saveDayEntries()` — opt-in checkbox in review modal, default unchecked. Correct.
- `publishEvent()` — opt-in checkbox in publish dialog, default unchecked. Correct.
- Metadata-only changes do not trigger notifications.
- Reorder changes are classified as substantive (can trigger notification if opted in).
- `DEBUG_NOTIFICATIONS` is env-driven (`false` by default), no longer hardcoded to `true`.
