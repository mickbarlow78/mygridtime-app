// Phase 7: Consumer (driver/parent) auth guard and layout.
// Placeholder — do not build until Phase 7.
export default function ConsumerLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <div>{children}</div>
}
