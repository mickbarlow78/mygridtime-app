'use client'

// Phase 2: Handles Supabase auth callback.
// Exchanges the auth code for a session, then redirects to /admin.
export default function AuthCallbackPage() {
  return (
    <div className="p-8">
      <p className="text-gray-500">Completing sign in&hellip;</p>
    </div>
  )
}
